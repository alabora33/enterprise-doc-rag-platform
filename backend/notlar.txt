========================================================
KURUMSAL DOKÜMAN RAG PLATFORMU — PROJE NOTLARI
========================================================

## PROJE FİKRİ
Şirketler PDF, Word, Excel, sözleşme, teknik doküman, prosedür,
yönetmelik yüklesin. Sistem bu dokümanları okusun ve kullanıcı
sorularına kaynaklı cevap versin.

Örnek:
  Kullanıcı : Bu sözleşmede fesih şartları nelerdir?
  Sistem     : Madde 8.2'ye göre taraflar 30 gün önceden yazılı
               bildirimle... | Kaynak: contract.pdf, sayfa 12

========================================================
KULLANILAN TEKNOLOJİLER
========================================================

Backend   : FastAPI, PostgreSQL, SQLAlchemy, Redis, Celery, Docker
AI        : OpenAI API veya local LLM, LangChain/LlamaIndex,
            Qdrant veya pgVector, Embedding model, RAG pipeline
Dosya     : PDF parser, DOCX parser, Excel reader, OCR (opsiyonel)
Frontend  : React veya basit admin panel

========================================================
MODÜLLER
========================================================
- Kullanıcı kayıt/giriş
- Şirket/organizasyon yapısı
- Dosya yükleme
- Doküman işleme kuyruğu
- Chunk oluşturma
- Embedding çıkarma
- Vector DB kaydı
- Chat ekranı + kaynak gösterme
- Token kullanım takibi
- Admin panel

========================================================
MİMARİ NEDEN BÖYLE KURULDU? (config.py bağlamı)
========================================================

1. pydantic-settings + BaseSettings kullanımı
   - Tüm hassas bilgiler (DB şifreleri, API key, JWT secret)
     kod içine gömülmeden .env dosyasından okunur.
   - Tip güvenliği: "8080" string'i otomatik int'e dönüşür,
     eksik zorunlu alan varsa uygulama hiç başlamaz.
   - Singleton pattern: settings nesnesi bir kez oluşturulur,
     projenin her yerinden aynı nesneye erişilir.

2. Neden bu alanlar var?
   - DATABASE_URL  : PostgreSQL bağlantısı (SQLAlchemy için)
   - REDIS_URL     : Celery broker + cache (kuyruk sistemi)
   - JWT_SECRET_KEY: Kullanıcı oturum token'larını imzalar
   - OPENAI_API_KEY: RAG pipeline'da LLM çağrısı için (opsiyonel,
                     local LLM kullanılacaksa boş bırakılabilir)
   - APP_ENV       : development/production ayrımı için

3. extra="ignore" neden var?
   - .env dosyasında olup sınıfta tanımlanmayan değişkenler
     hata vermeden geçilir. Ortam genişledikçe esneklik sağlar.

4. Genel mimari mantığı
   - FastAPI  : Async HTTP API katmanı
   - Celery   : Doküman işleme ağır iş yüklerini arka plana atar
                (PDF parse, embedding çıkarma vs.)
   - Redis    : Celery'nin mesaj kuyruğu + oturum cache'i
   - pgVector : Embedding'leri PostgreSQL içinde saklar,
                Qdrant ise ayrı vector DB olarak kullanılabilir
   - RAG akışı: Yüklenen dosya → chunk → embedding → vector DB
                Soru gelince → embedding → benzer chunk bul
                → LLM'e context ile gönder → kaynaklı cevap dön

========================================================

========================================================
SESSION.PY — VERİTABANI OTURUM YÖNETİMİ
========================================================

## NE YAPAR?
PostgreSQL bağlantısını yönetir. SQLAlchemy engine'i oluşturur,
session factory tanımlar ve FastAPI endpoint'lerine DB session'ı
Dependency Injection ile sağlar.

## SATIR SATIR
- create_engine(DATABASE_URL, pool_pre_ping=True)
    Bağlantı havuzu oluşturur. pool_pre_ping ölü bağlantıları
    kullanmadan önce test eder (production'da kritik).

- SessionLocal = sessionmaker(autocommit=False, autoflush=False)
    autocommit=False → manuel commit gerekir, hata olursa rollback
    autoflush=False  → sorgu öncesi otomatik flush yok (performans)

- get_db() generator fonksiyonu
    FastAPI Depends() ile kullanılır. yield ile session endpoint'e
    verilir, istek bitince (hata olsa bile) finally ile kapatılır.
    Bağlantı sızıntısı olmaz.

========================================================
SQLALCHEMY NEDEN KULLANILDI?
========================================================

TEMEL AVANTAJLAR
- ORM: Tablolar Python sınıfı, satırlar Python nesnesi olur
- SQL Injection koruması: Parametreler otomatik escape edilir
- DB bağımsızlığı: PostgreSQL/MySQL/SQLite aynı kodla çalışır
- Relationship yönetimi: JOIN yazmak yerine user.documents gibi
- Connection pool: Her request için yeni bağlantı açılmaz
- Alembic entegrasyonu: Model değişince migration otomatik üretilir

BU PROJEDE NEDEN GEREKLİ?
- Kullanıcı / şirket / doküman ilişkileri → relationship
- Doküman işleme durumu takibi → güvenli transaction
- Token kullanım takibi → atomic sayaç güncellemeleri
- Celery worker'lar DB yazıyor → her worker'da ayrı session
- Şema değişiklikleri → alembic revision --autogenerate

NEDEN ALTERNATİFLER SEÇİLMEDİ?
- psycopg2   : Çok düşük seviye, her şey elle
- Tortoise   : Async-only, ekosistemi küçük
- SQLModel   : Henüz olgunlaşmamış
- SQLAlchemy : Endüstri standardı, en geniş ekosistem, battle-tested



========================================================
SESSIONLOCAL — autocommit=False ve autoflush=False
========================================================

## autocommit=False — Neden?

Veritabanında her işlem bir "transaction" içinde gerçekleşir.
autocommit=True olsaydı her SQL cümlesi anında DB'ye yazılırdı,
geri alınamaz ve hata yönetimi imkansız olurdu.

autocommit=False ile:
  - Değişiklikler db.commit() çağrılana kadar DB'ye yazılmaz
  - Hata olursa db.rollback() ile her şey geri alınır
  - Birden fazla işlem atomik yapılabilir (ya hepsi ya hiçbiri)

Örnek senaryo — doküman yükleme:
  1. Dosya kaydı DB'ye eklendi
  2. Celery kuyruğuna görev eklendi
  3. Kullanım sayacı güncellendi
  → 2. adımda hata olursa commit yapılmaz,
    yarım kayıt DB'de kalmaz, rollback ile temizlenir.

## autoflush=False — Neden?

flush = session belleğindeki değişiklikleri SQL'e çevirmek
        (ama henüz commit etmemek)

autoflush=True olsaydı her db.query() çağrısından önce
SQLAlchemy otomatik flush yapardı. Bu:
  - Beklenmedik DB yazımlarına yol açar
  - Henüz hazır olmayan veriyi DB'ye gönderebilir
  - Özellikle Celery worker'lar ve karmaşık işlemlerde
    sıra dışı davranışlara neden olur

autoflush=False ile:
  - Flush kontrolü tamamen geliştiricinin elinde
  - İstersen db.flush() ile erken flush yapabilirsin
  - Performans: gereksiz SQL round-trip olmaz

## ÖZET

  autocommit=False + autoflush=False = maksimum kontrol
  Kurumsal projede veri tutarlılığı performanstan önce gelir.
  RAG pipeline'da doküman → chunk → embedding → vector DB
  zinciri gibi çok adımlı işlemlerde bu kontrol hayati.

========================================================
========================================================
BASE.PY — TÜM MODELLERİN ATA SINIFI
========================================================

from sqlalchemy.orm import DeclarativeBase

class Base(DeclarativeBase):
    pass

- SQLAlchemy 2.0'ın modern ORM sistemi.
  Eski declarative_base() fonksiyonunun yerini aldı.
- Tüm modeller bu sınıftan kalıtım alır (User(Base) gibi).
- pass: şu an ekstra davranış gerekmiyor, saf temel sınıf.

NEDEN AYRI DOSYA?
- Alembic migration'ları için tüm modellerin aynı Base'den
  türemesi zorunlu. Base olmadan migration çalışmaz.
- Circular import önlenir: modeller base.py'ı import eder,
  session.py modelleri import etmez.

========================================================
USER.PY — KULLANICI MODELİ (users tablosu)
========================================================

__tablename__ = "users"
  → PostgreSQL'deki tablo adı

id: Mapped[int] — primary_key=True, index=True
  → Her satırın benzersiz kimliği. index ile WHERE id=X hızlı.
  → Mapped[int]: Python tip sistemi bilir, IDE autocomplete çalışır.

email: Mapped[str] — unique=True, index=True, nullable=False
  → Aynı email iki kez kayıt olamaz (unique).
  → Login sorgularında hız için index.
  → String(255): sınırsız bırakmak yerine alan kısıtı.

full_name: Mapped[str | None] — nullable=True
  → Opsiyonel alan. Python'da None, DB'de NULL olabilir.

hashed_password: Mapped[str] — nullable=False
  → Plain text şifre ASLA saklanmaz.
  → bcrypt/argon2 ile hash'lenmiş hali tutulur.
  → Alan adı da bunu açıkça belirtir (güvenlik prensibi).

is_active: Mapped[bool] — default=True
  → Soft delete mantığı: kullanıcı silme yerine is_active=False.
  → Kayıt DB'den silinmez, audit trail korunur.

is_superuser: Mapped[bool] — default=False
  → Admin panel ve yetkilendirme için.
  → Yeni kullanıcılar varsayılan False — güvenlik gereği.

created_at: Mapped[datetime] — default=datetime.utcnow
  → Kayıt ilk oluşturulduğunda bir kez set edilir.

updated_at: Mapped[datetime] — onupdate=datetime.utcnow
  → Her güncellemede otomatik değişir.
  → UTC kullanımı: tüm sunucular aynı zaman diliminde çalışır.

========================================================
MODELS/__INIT__.PY — MODEL DIŞA AÇMA
========================================================

from app.models.user import User
__all__ = ["User"]

- "from app.models import User" şeklinde kısa import imkanı sağlar.
- __all__: bu modülden * ile import edilince sadece User çıkar,
  iç detaylar gizli kalır.
- Alembic modelleri keşfedebilmek için bir yerden import
  edilmesini bekler. Bu dosya o işi yapar.

========================================================
MİMARİ BAĞLANTI — DOSYALAR ARASI İLİŞKİ
========================================================

base.py       →  Base sınıfı (tüm modellerin atası)
    ↓
user.py       →  User(Base) — "users" tablosunu tanımlar
    ↓
__init__.py   →  Modelleri dışa açar, Alembic keşfeder
    ↓
alembic/      →  Base.metadata'ya bakarak migration üretir
    ↓
session.py    →  Engine + SessionLocal ile DB'ye bağlanır

========================================================
========================================================
ALEMBİC — VERİTABANI MİGRATION YÖNETİMİ
========================================================

## NEDİR?
SQLAlchemy modellerini okuyarak veritabanı şema değişikliklerini
otomatik üreten, versiyonlayan ve uygulayan araç.
Django'daki makemigrations/migrate ikilisinin SQLAlchemy karşılığı.

## NEDEN KULLANDIK?
Ham SQL ile şema yönetmek kaosa yol açar:
  - Hangi sunucuda hangi ALTER TABLE çalıştırıldı?
  - Takım arkadaşı bu değişikliği aldı mı?
  - Hata olursa geri alınabilir mi?
Alembic tüm bunları çözer.

AVANTAJLAR
- Migration dosyaları Git'e eklenir → takım senkron kalır
- Her migration versiyonlanır → DB'nin hangi versiyonda
  olduğu alembic_version tablosunda tutulur
- --autogenerate: model değişince SQL'i sen yazmıyorsun
- upgrade/downgrade: ileri ve geri alınabilir
- Üretime güvenle deploy edilebilir

========================================================
YAPILAN YAPILANDIRMALAR VE NEDENLERİ
========================================================

## 1. alembic.ini — sqlalchemy.url değiştirildi

  Eskisi : sqlalchemy.url = driver://user:pass@localhost/dbname
  Yenisi : sqlalchemy.url = postgresql://rag_user:rag_password@localhost:5432/rag_db

  Alembic DB'ye bağlanmak için bu URL'i kullanır.
  localhost kullanıldı çünkü alembic komutu Windows
  terminalinden (Docker dışından) çalıştırılıyor.
  Docker container içinden çalışsaydı "db" yazılacaktı.

## 2. alembic/env.py — iki kritik ekleme yapıldı

  EKLEME 1 — Model import'ları:
    from app.db.base import Base
    from app.models import User

    Alembic modelleri görebilmek için bunları import etmek
    zorunda. Import edilmeden Base.metadata boş kalır,
    autogenerate hiçbir tablo üretemez.

  EKLEME 2 — target_metadata:
    Eskisi : target_metadata = None
    Yenisi : target_metadata = Base.metadata

    Base.metadata tüm model tanımlarını içerir. Alembic buna
    bakarak "DB'de ne olmalı?" sorusunu yanıtlar ve mevcut
    DB şemasıyla farkı bulur, migration'ı otomatik yazar.

========================================================
TEMEL ALEMBİC KOMUTLARI
========================================================

Migration oluştur (otomatik):
  alembic revision --autogenerate -m "create users table"
  → app/models içindeki modellere bakarak SQL üretir
  → alembic/versions/xxxx_create_users_table.py dosyası oluşur

Migration'ı uygula:
  alembic upgrade head
  → "head" = en son migration versiyonu
  → SQL PostgreSQL'e uygulanır, tablo fiziksel olarak oluşur
  → alembic_version tablosuna versiyon ID yazılır

Geri al:
  alembic downgrade -1   → bir adım geri
  alembic downgrade base → en başa dön

Mevcut durumu gör:
  alembic current        → DB hangi versiyonda?
  alembic history        → tüm migration geçmişi

========================================================
PROJEDEKİ YERİ — BÜYÜK RESİM
========================================================

Yeni model eklendi (Document, Chunk, Organization...)
        ↓
alembic revision --autogenerate -m "add documents table"
        ↓
alembic/versions/ içinde yeni dosya oluştu
        ↓
alembic upgrade head
        ↓
PostgreSQL şeması güncellendi
        ↓
Git'e commit → takım pull → aynı komutu çalıştırır → senkron

İleride eklenecek tablolar: Document, Chunk, Embedding,
Organization, TokenUsage — hepsi Alembic ile yönetilecek.

========================================================

========================================================
SECURITY.PY — ŞİFRE VE JWT TOKEN YÖNETİMİ
========================================================

## GENEL AMAÇ
Kullanıcı kimlik doğrulamasının iki temel parçasını yönetir:
  1. Şifre hash'leme ve doğrulama (bcrypt)
  2. JWT token üretme ve çözme (python-jose)

========================================================
KÜTÜPHANELER
========================================================

passlib + bcrypt:
  - Şifre hash'lemek için kullanılır
  - bcrypt kasıtlı olarak YAVAŞ çalışır → brute-force zorlaşır
  - MD5/SHA gibi hızlı algoritmalar şifre için tehlikelidir

python-jose:
  - JWT (JSON Web Token) üretmek ve doğrulamak için
  - JWT: kullanıcı kimliğini şifreli ve imzalı taşıyan string
  - Sunucu session tutmaz, token her istekte doğrulanır

========================================================
ŞİFRE YÖNETİMİ
========================================================

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
  - bcrypt algoritmasını aktif eder
  - deprecated="auto": ileride algoritma değişirse eski
    hash'ler otomatik işaretlenir, sistem bozulmaz

verify_password(plain_password, hashed_password) → bool
  - Login sırasında kullanılır
  - Kullanıcının girdiği şifreyi DB'deki hash ile karşılaştırır
  - Plain text asla DB'de tutulmaz, karşılaştırma hash üzerinden

get_password_hash(password) → str
  - Kayıt sırasında kullanılır
  - Şifreyi bcrypt ile hash'ler
  - Her çağrıda farklı SALT üretilir → aynı şifre her seferinde
    farklı hash verir → rainbow table saldırısı engellenir

========================================================
JWT TOKEN ÜRETİMİ — create_access_token
========================================================

subject: str | int
  - Genellikle user.id — token kimin için üretildi

expires_delta: timedelta | None
  - Özel süre belirlenebilir, belirtilmezse config'deki
    ACCESS_TOKEN_EXPIRE_MINUTES (60 dk) kullanılır

datetime.now(timezone.utc)
  - Timezone-aware UTC zamanı kullanılır
  - Sunucunun lokal saatinden bağımsız
  - Farklı ülkelerdeki sunucularda tutarsızlık olmaz

JWT Payload (to_encode):
  "exp" → token'ın biteceği zaman (RFC 7519 standardı)
          kütüphane bunu otomatik kontrol eder
  "sub" → token sahibinin kimliği (user id string olarak)

jwt.encode(payload, JWT_SECRET_KEY, algorithm=JWT_ALGORITHM)
  - Payload secret key ile İMZALANIR
  - İmza olmadan herkes payload'ı değiştirebilir
    ("sub": "1" → "sub": "999" gibi yetki yükseltme)
  - Secret key ile imzalanınca sunucu değişikliği anlar

========================================================
JWT TOKEN ÇÖZME — decode_access_token
========================================================

Her korumalı API endpoint'inde çalışır.
jwt.decode() aynı anda 3 şeyi kontrol eder:
  1. İmza geçerli mi?     → secret key ile doğrular
  2. Süre dolmuş mu?      → "exp" claim otomatik kontrol edilir
  3. Algoritma doğru mu?  → farklı algoritmayı reddeder

Herhangi biri başarısız → JWTError → None döner → istek reddedilir
Başarılı → payload dict döner → payload["sub"] = user.id

========================================================
SALDIRI KORUMALARI
========================================================

DB sızıntısı        → bcrypt hash, plain şifre yok
Rainbow table       → her hash'e farklı salt
Token sahteciliği   → JWT imzası (secret key)
Süresi dolmuş token → "exp" claim otomatik kontrol
Algoritma karıştırma → algorithms=[...] ile kısıtlama
Brute-force         → bcrypt'in kasıtlı yavaşlığı

========================================================
KULLANıM AKIŞI
========================================================

KAYIT:
  kullanıcı şifre girer
      ↓
  get_password_hash() → bcrypt hash
      ↓
  hash DB'ye kaydedilir (plain şifre asla)

GİRİŞ:
  kullanıcı email + şifre gönderir
      ↓
  DB'den hash çekilir
      ↓
  verify_password() → eşleşirse True
      ↓
  create_access_token(user.id) → JWT token
      ↓
  token kullanıcıya gönderilir

KORUNAN ENDPOINT:
  istek → Authorization: Bearer <token>
      ↓
  decode_access_token() → payload veya None
      ↓
  payload["sub"] → user.id → DB'den kullanıcı çekilir
      ↓
  işlem yapılır veya 401 döner

========================================================

========================================================
SCHEMAS/USER.PY — API GİRİŞ/ÇIKIŞ ŞEMALARI
========================================================

## GENEL AMAÇ
models/user.py DB tablosunu tanımlar.
schemas/user.py API'nin ne alıp ne vereceğini tanımlar.
İkisi birbirinden kasıtlı olarak ayrıdır.

  models/user.py  → SQLAlchemy → DB katmanı → tüm kolonlar
  schemas/user.py → Pydantic   → HTTP katmanı → sadece API'ye açık alanlar

## UserCreate — Kayıt İsteği Şeması
POST /auth/register endpoint'ine gelen body bu şemaya göre
validate edilir.

  email: EmailStr
    → Sadece str değil, format kontrolü yapılır
    → "abc" veya "ali@" gelirse otomatik 422 döner

  password: str = Field(min_length=6, max_length=128)
    → min_length=6  : çok kısa şifre reddedilir
    → max_length=128: bcrypt'i DoS'lamak engellenir
    → Şifre buradan gelir ama DB'ye ASLA yazılmaz
    → security.py get_password_hash() ile hash'lenerek kaydedilir

  full_name: str | None = None
    → Opsiyonel, gönderilmezse None, kayıt yine kabul edilir

## UserRead — Kayıt Yanıtı Şeması
GET /users/me gibi endpoint'lerin response şeması.
API'nin kullanıcıya geri döndürdüğü veriyi temsil eder.

  Alanlar: id, email, full_name, is_active, is_superuser, created_at

  KRİTİK: hashed_password yok.
  DB'deki User modelinde bu alan var ama şemaya dahil edilmedi.
  API asla şifre hash'ini dışarı sızdırmaz. Bilinçli güvenlik kararı.

  model_config = {"from_attributes": True}
    → Pydantic v2 ayarı
    → Olmadan sadece dict'ten okuyabilir
    → Bu ayarla SQLAlchemy ORM nesnesinden doğrudan okur
    → db.query(User).first() → UserRead.model_validate(db_user) çalışır

## NEDEN AYRI SINIFLAR?
  İstek → UserCreate validate eder → iş mantığı
                                          ↓
                                  DB'den User nesnesi
                                          ↓
                          UserRead filtreler → response

  UserCreate : Gelen veriyi validate et, şifreyi kabul et
  UserRead   : Giden veriyi filtrele, hassas alanları gizle

========================================================
SCHEMAS/TOKEN.PY — JWT TOKEN ŞEMALARI
========================================================

## Token — Login Response Şeması
Login başarılı olunca API'nin döndürdüğü response.

  access_token: str
    → create_access_token() ile üretilen JWT string

  token_type: str = "bearer"
    → OAuth2 standardı, sabit "bearer" değeri
    → Client bunu alıp her istekte şu header'ı gönderir:
      Authorization: Bearer <token>

  Örnek response:
    {
      "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6...",
      "token_type": "bearer"
    }

## TokenPayload — JWT İçi Veri Şeması
Token decode edilince içinden çıkan payload'ı temsil eder.

  sub: str | None = None
    → security.py'da token üretilirken "sub": str(user.id)
      olarak set edilen user.id değeri
    → None olabilir: token geçersizse uygulama patlamaz

  Kullanım:
    payload = decode_access_token(token)
    token_data = TokenPayload(**payload)
    user_id = token_data.sub  # "42" gibi

## İKİ SINIFIN FARKI
  Token        → Login response'u dönerken  (API → Client)
  TokenPayload → Token doğrulanırken        (JWT içi → Uygulama)

========================================================
GENEL ŞEMA MİMARİSİ
========================================================

LOGIN AKIŞI:
  POST /auth/login (email + password)
      ↓
  UserCreate ile validate edilir
      ↓
  verify_password() → doğruysa
      ↓
  create_access_token(user.id)
      ↓
  Token şeması → {"access_token": "...", "token_type": "bearer"}

KORUNAN ENDPOINT AKIŞI:
  GET /users/me + Authorization: Bearer <token>
      ↓
  decode_access_token() → TokenPayload → sub = user.id
      ↓
  DB'den kullanıcı çekilir
      ↓
  UserRead şeması filtreler → response (şifre hash'i yok)

========================================================

========================================================
SERVICES/USER_SERVICE.PY — KULLANICI İŞ MANTIĞI
========================================================

## NEDEN AYRI BİR SERVICE KATMANI?

  API endpoint  →  service       →  DB (model)
  (HTTP katmanı)   (iş mantığı)     (veri katmanı)

  Endpoint sadece HTTP işleriyle ilgilenir (istek al, response dön).
  İş mantığı service'te olur.
  Avantajlar:
    - Aynı mantık birden fazla endpoint'ten çağrılabilir
    - Test edilmesi kolaylaşır
    - Katmanlar birbirinden bağımsız değiştirilebilir

========================================================
FONKSİYONLAR
========================================================

## get_user_by_email(db, email) → User | None
  db.query(User).filter(User.email == email).first()
  → SELECT * FROM users WHERE email = ? LIMIT 1
  → Kayıt yoksa None döner
  Kullanım: login + "email zaten kayıtlı mı?" kontrolü

## get_user_by_id(db, user_id) → User | None
  db.query(User).filter(User.id == user_id).first()
  → Token decode edilince sub alanından gelen user.id ile
    DB'den kullanıcı çekmek için kullanılır
  Kullanım: GET /users/me gibi korumalı endpoint'ler

## create_user(db, user_in: UserCreate) → User
  Adımlar sırasıyla:

  1. hashed_password = get_password_hash(user_in.password)
       → İlk iş: plain text şifre hemen hash'lenir
       → Bundan sonra plain şifre bir daha kullanılmaz

  2. user = User(email=..., hashed_password=..., ...)
       → ORM nesnesi oluşturulur
       → is_active=True  : yeni kullanıcı aktif başlar
       → is_superuser=False : sabit, güvenlik gereği
         (yeni kullanıcı asla admin olamaz)

  3. db.add(user)
       → Session'a eklenir, henüz DB'ye yazılmadı

  4. db.commit()
       → Transaction tamamlanır, INSERT SQL çalışır
       → Veri DB'ye fiziksel olarak yazılır

  5. db.refresh(user)
       → DB'nin atadığı id, created_at, updated_at
         değerleri Python nesnesine geri yüklenir
       → Bu olmadan user.id hala None olurdu

  6. return user
       → Endpoint bunu UserRead şemasına çevirir

## authenticate_user(db, email, password) → User | None
  Login işleminin tek fonksiyonu. Ya User döner ya None.

  user = get_user_by_email(db, email)
  if not user: return None
    → Email DB'de yok

  if not verify_password(password, user.hashed_password): return None
    → Şifre yanlış

  KRİTİK GÜVENLİK KARARI:
  "Email bulunamadı" ve "şifre yanlış" için AYRI mesaj verilmez.
  İkisi de None döner. Neden?
  → Saldırgan hangi email'in kayıtlı olduğunu anlayamasın
  → User Enumeration saldırısı engellenir

========================================================
KATMANLARIN BİRLİKTE ÇALIŞMASI
========================================================

POST /auth/register:
  UserCreate (validate) → create_user()
    ├── get_password_hash()        ← security.py
    ├── User(hashed_password=...)  ← models/user.py
    ├── db.add / commit / refresh  ← session.py
    └── User nesnesi döner
  UserRead (filtrele) → response

POST /auth/login:
  authenticate_user(db, email, password)
    ├── get_user_by_email()
    ├── verify_password()          ← security.py
    └── User veya None döner
  create_access_token(user.id)     ← security.py
  Token şeması → response

GET /users/me:
  decode_access_token(token) → sub = user.id
  get_user_by_id(db, user_id)
  UserRead (filtrele) → response

========================================================
========================================================
API/V1/AUTH.PY — KİMLİK DOĞRULAMA ENDPOINT'LERİ
========================================================

## GENEL AMAÇ
Register ve login HTTP endpoint'lerini barındırır.
İş mantığı YAPMAZ — koordine eder.
  Validasyon    → Pydantic şemalar
  İş mantığı   → user_service.py
  DB bağlantısı → session.py
  Güvenlik      → security.py
  Auth.py       → bunları bir araya getirir, hata yönetir

========================================================
ROUTER TANIMI
========================================================

router = APIRouter(prefix="/auth", tags=["Auth"])
  - prefix="/auth" → tüm endpoint'ler /auth/... ile başlar
  - tags=["Auth"]  → Swagger UI'da "Auth" grubu altında görünür

========================================================
POST /auth/register
========================================================

@router.post("/register", response_model=UserRead, status_code=201)

  response_model=UserRead
    → Dönüş UserRead şemasından geçirilir
    → hashed_password otomatik çıkarılır, dışarı sızmaz

  status_code=201
    → Kaynak oluşturulunca standart HTTP kodu 201'dir (200 değil)

PARAMETRELER:
  user_in: UserCreate
    → Request body otomatik parse ve validate edilir
    → Geçersiz email veya kısa şifre → endpoint koda girmeden 422 döner

  db: Session = Depends(get_db)
    → get_db() generator çalışır, session enjekte edilir
    → İstek bitince otomatik kapatılır

AKIM:
  1. get_user_by_email() → email kayıtlı mı kontrol et
  2. Kayıtlıysa → 400 Bad Request + açıklayıcı mesaj
     (DB unique kısıtı da engeller ama bu daha önce çalışır,
      kullanıcıya anlamlı mesaj verir)
  3. create_user() → service katmanına devret
  4. Dönen User nesnesi → UserRead filtreler → response

========================================================
POST /auth/login
========================================================

@router.post("/login", response_model=Token)
  → Response: {"access_token": "...", "token_type": "bearer"}

PARAMETRELER:
  form_data: OAuth2PasswordRequestForm = Depends()
    → FastAPI built-in OAuth2 form şeması
    → application/x-www-form-urlencoded formatı bekler (OAuth2 standardı)
    → Email gönderilse de alan adı "username"dir — standart böyle
    → form_data.username ve form_data.password ile erişilir

  db: Session = Depends(get_db)
    → DB session enjekte edilir

AKIM:
  1. authenticate_user() → email + şifre doğrula
     → None dönerse:
       401 Unauthorized + "Incorrect email or password."
       headers={"WWW-Authenticate": "Bearer"}
         → OAuth2 standardı zorunlu kılar
         → Tarayıcıya "bearer token bekliyorum" sinyali
       KRİTİK: "email mi yanlış şifre mi?" söylenmiyor
         → User Enumeration saldırısı engellenir

  2. is_active kontrolü
     → False ise 400 Bad Request + "Inactive user."
     → Soft-delete: hesap silinmez, devre dışı bırakılır
     → Devre dışı hesaba token üretilmez

  3. Token süresi config'den okunur (ACCESS_TOKEN_EXPIRE_MINUTES)
     create_access_token(subject=user.id, expires_delta=...)
     → user.id token'ın "sub" alanına yazılır

  4. Token(access_token=..., token_type="bearer") → response

========================================================
HTTP DURUM KODLARI VE ANLAMLARI
========================================================

201 Created      → Kayıt başarılı, yeni kaynak oluşturuldu
400 Bad Request  → Email zaten kayıtlı / hesap pasif
401 Unauthorized → Email veya şifre yanlış
422 Unprocessable→ Geçersiz veri (Pydantic otomatik üretir)

========================================================
GENEL MİMARİ AKIŞ
========================================================

HTTP isteği gelir
      ↓
FastAPI → Pydantic şema validate eder
      ↓
Endpoint → service fonksiyonu çağırır
      ↓
Service → DB işlemi yapar, sonuç döner
      ↓
Endpoint → hata kontrolü (HTTPException)
      ↓
Pydantic → response_model ile çıktıyı filtreler
      ↓
Client'a response gider

========================================================
TÜM SİSTEMİN REGISTER/LOGIN AKIŞI
========================================================

REGISTER:
  POST /auth/register (body: email + password)
      ↓
  UserCreate validate → email format, şifre uzunluğu
      ↓
  Email kayıtlı mı? → Evet: 400 Hayır: devam
      ↓
  get_password_hash() → bcrypt hash
      ↓
  db.add/commit/refresh → users tablosuna INSERT
      ↓
  UserRead filtrele → 201 + kullanıcı bilgileri (şifresiz)

LOGIN:
  POST /auth/login (form: username + password)
      ↓
  authenticate_user() → email bul + verify_password()
      ↓
  Başarısız → 401 | Pasif hesap → 400
      ↓
  create_access_token(user.id) → JWT imzalanır
      ↓
  Token → {"access_token": "eyJ...", "token_type": "bearer"}

========================================================

========================================================
API/V1/AUTH.PY — KİMLİK DOĞRULAMA ENDPOINT'LERİ
========================================================

## GENEL AMAÇ
Register ve login HTTP endpoint'lerini barındırır.
İş mantığı YAPMAZ — koordine eder.
  Validasyon    → Pydantic şemalar
  İş mantığı   → user_service.py
  DB bağlantısı → session.py
  Güvenlik      → security.py
  Auth.py       → bunları bir araya getirir, hata yönetir

========================================================
ROUTER TANIMI
========================================================

router = APIRouter(prefix="/auth", tags=["Auth"])
  - prefix="/auth" → tüm endpoint'ler /auth/... ile başlar
  - tags=["Auth"]  → Swagger UI'da "Auth" grubu altında görünür

========================================================
POST /auth/register
========================================================

@router.post("/register", response_model=UserRead, status_code=201)

  response_model=UserRead
    → Dönüş UserRead şemasından geçirilir
    → hashed_password otomatik çıkarılır, dışarı sızmaz

  status_code=201
    → Kaynak oluşturulunca standart HTTP kodu 201'dir (200 değil)

PARAMETRELER:
  user_in: UserCreate
    → Request body otomatik parse ve validate edilir
    → Geçersiz email veya kısa şifre → endpoint koda girmeden 422 döner

  db: Session = Depends(get_db)
    → get_db() generator çalışır, session enjekte edilir
    → İstek bitince otomatik kapatılır

AKIM:
  1. get_user_by_email() → email kayıtlı mı kontrol et
  2. Kayıtlıysa → 400 Bad Request + açıklayıcı mesaj
     (DB unique kısıtı da engeller ama bu daha önce çalışır,
      kullanıcıya anlamlı mesaj verir)
  3. create_user() → service katmanına devret
  4. Dönen User nesnesi → UserRead filtreler → response

========================================================
POST /auth/login
========================================================

@router.post("/login", response_model=Token)
  → Response: {"access_token": "...", "token_type": "bearer"}

PARAMETRELER:
  form_data: OAuth2PasswordRequestForm = Depends()
    → FastAPI built-in OAuth2 form şeması
    → application/x-www-form-urlencoded formatı bekler (OAuth2 standardı)
    → Email gönderilse de alan adı "username"dir — standart böyle
    → form_data.username ve form_data.password ile erişilir

  db: Session = Depends(get_db)
    → DB session enjekte edilir

AKIM:
  1. authenticate_user() → email + şifre doğrula
     → None dönerse:
       401 Unauthorized + "Incorrect email or password."
       headers={"WWW-Authenticate": "Bearer"}
         → OAuth2 standardı zorunlu kılar
         → Tarayıcıya "bearer token bekliyorum" sinyali
       KRİTİK: "email mi yanlış şifre mi?" söylenmiyor
         → User Enumeration saldırısı engellenir

  2. is_active kontrolü
     → False ise 400 Bad Request + "Inactive user."
     → Soft-delete: hesap silinmez, devre dışı bırakılır
     → Devre dışı hesaba token üretilmez

  3. Token süresi config'den okunur (ACCESS_TOKEN_EXPIRE_MINUTES)
     create_access_token(subject=user.id, expires_delta=...)
     → user.id token'ın "sub" alanına yazılır

  4. Token(access_token=..., token_type="bearer") → response

========================================================
HTTP DURUM KODLARI VE ANLAMLARI
========================================================

201 Created      → Kayıt başarılı, yeni kaynak oluşturuldu
400 Bad Request  → Email zaten kayıtlı / hesap pasif
401 Unauthorized → Email veya şifre yanlış
422 Unprocessable→ Geçersiz veri (Pydantic otomatik üretir)

========================================================
GENEL MİMARİ AKIŞ
========================================================

HTTP isteği gelir
      ↓
FastAPI → Pydantic şema validate eder
      ↓
Endpoint → service fonksiyonu çağırır
      ↓
Service → DB işlemi yapar, sonuç döner
      ↓
Endpoint → hata kontrolü (HTTPException)
      ↓
Pydantic → response_model ile çıktıyı filtreler
      ↓
Client'a response gider

========================================================
TÜM SİSTEMİN REGISTER/LOGIN AKIŞI
========================================================

REGISTER:
  POST /auth/register (body: email + password)
      ↓
  UserCreate validate → email format, şifre uzunluğu
      ↓
  Email kayıtlı mı? → Evet: 400 Hayır: devam
      ↓
  get_password_hash() → bcrypt hash
      ↓
  db.add/commit/refresh → users tablosuna INSERT
      ↓
  UserRead filtrele → 201 + kullanıcı bilgileri (şifresiz)

LOGIN:
  POST /auth/login (form: username + password)
      ↓
  authenticate_user() → email bul + verify_password()
      ↓
  Başarısız → 401 | Pasif hesap → 400
      ↓
  create_access_token(user.id) → JWT imzalanır
      ↓
  Token → {"access_token": "eyJ...", "token_type": "bearer"}

========================================================

========================================================
API/V1/USERS.PY — KULLANICI ENDPOINT'LERİ VE KİMLİK DOĞRULAMA
========================================================

## GENEL AMAÇ
İki görevi var:
  1. get_current_user: token doğrulayan yeniden kullanılabilir dependency
  2. GET /users/me: giriş yapmış kullanıcının bilgilerini döner

========================================================
OAUTH2 SCHEME
========================================================

oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/v1/auth/login")
  - Her istekte Authorization: Bearer <token> header'ını okur
  - Token string'ini otomatik çıkarır
  - tokenUrl: Swagger UI Authorize butonunu bu endpoint'e yönlendirir
  - Depends(oauth2_scheme) ile kullanılır

========================================================
get_current_user — KİMLİK DOĞRULAMA DEPENDENCY'Sİ
========================================================

Endpoint değil, dependency fonksiyonu.
Depends(get_current_user) yazılan her endpoint bu kodu çalıştırır.
Tekrar tekrar yazmak gerekmez.

PARAMETRELER:
  token: str = Depends(oauth2_scheme)
    → Authorization header'dan token otomatik alınır
  db: Session = Depends(get_db)
    → DB session enjekte edilir

KONTROL ADIMLARI (sırasıyla):

  1. decode_access_token(token)
     → None dönerse: 401 "Invalid authentication credentials."
     → Token geçersiz, süresi dolmuş veya imzası bozuk
     → Üç farklı hata aynı mesajla döner (hangisi olduğu söylenmez)

  2. payload.get("sub")
     → None dönerse: 401 "Invalid token payload."
     → .get() kullanılır — payload["sub"] KeyError fırlatır, güvensiz
     → Token geçerli ama içinde sub yoksa bozuk token

  3. get_user_by_id(db, int(user_id))
     → None dönerse: 401 "User not found."
     → Token geçerli ama kullanıcı silinmiş olabilir
     → Her istekte DB'den kontrol yapılır
     → Silinen kullanıcı token süresi dolana kadar erişemez

  4. is_active kontrolü
     → False ise: 400 "Inactive user."
     → Soft-delete: hesap silinmez, devre dışı bırakılır

  5. return user
     → Tüm kontroller geçildi, User ORM nesnesi döner

========================================================
GET /users/me
========================================================

@router.get("/me", response_model=UserRead)
def read_current_user(current_user=Depends(get_current_user)):
    return current_user

  - Endpoint'in kendi kodu tek satır: return current_user
  - Tüm iş Depends(get_current_user) ile yapıldı
  - response_model=UserRead → hashed_password otomatik çıkarılır

========================================================
DEPENDS ZİNCİRİ — TAM AKIŞ
========================================================

GET /users/me isteği gelir
      ↓
Depends(get_current_user) tetiklenir
      ↓
  Depends(oauth2_scheme) → Authorization header'dan token
  Depends(get_db)        → DB session açılır
      ↓
  decode_access_token()  → payload
  payload["sub"]         → user_id
  get_user_by_id()       → User nesnesi
  is_active kontrolü
      ↓
current_user = User nesnesi
      ↓
return current_user → UserRead filtreler → response

========================================================
NEDEN BU TASARIM? (Reusable Dependency)
========================================================

get_current_user bir kez yazıldı.
İleride tüm korumalı endpoint'ler sadece şunu ekler:

  def upload_document(current_user=Depends(get_current_user)):
  def list_documents(current_user=Depends(get_current_user)):
  def start_chat(current_user=Depends(get_current_user)):

Kimlik doğrulama mantığı tek yerde, her endpoint'e dağıtılmaz.

========================================================
API/V1/ROUTER.PY — ANA ROUTER BİRLEŞTİRİCİ
========================================================

## GENEL AMAÇ
Tüm v1 router'larını tek bir api_router altında toplar.
main.py sadece bu tek router'ı bağlar.

api_router = APIRouter()
  → Kendi prefix'i yok, prefix main.py'da verilir (/api/v1)

api_router.include_router(auth.router)   → prefix /auth
api_router.include_router(users.router)  → prefix /users

OLUŞAN URL'LER:
  /api/v1/auth/register
  /api/v1/auth/login
  /api/v1/users/me

NEDEN BU YAPI?
  main.py'da tek satır yeterli:
    app.include_router(api_router, prefix="/api/v1")

  Yeni modül eklemek sadece iki satır:
    from app.api.v1 import documents
    api_router.include_router(documents.router)

ROUTER HİYERARŞİSİ:
  main.py → api_router (/api/v1)
                ├── auth.router  (/auth)  → /api/v1/auth/register
                │                         → /api/v1/auth/login
                └── users.router (/users) → /api/v1/users/me

  İleride eklenecekler:
                ├── documents.router (/documents)
                ├── chat.router      (/chat)
                └── admin.router     (/admin)

========================================================

========================================================
COMMIT: "Add JWT authentication endpoints" — ORGANİZASYON
VE KULLANICI ROL KATMANININ EKLENMESİ
========================================================

Bu commit ile sistemin multi-tenant (çok kiracılı) iskelet yapısı
kuruldu. Her kullanıcı kayıt olduğunda otomatik bir organizasyon
oluşturulur ve kullanıcı o organizasyonun OWNER'ı olur.

Etkilenen dosyalar (migration hariç):
  GÜNCELLEME : models/user.py
  GÜNCELLEME : models/__init__.py
  GÜNCELLEME : schemas/user.py
  GÜNCELLEME : services/user_service.py
  GÜNCELLEME : api/v1/router.py
  YENİ       : models/organization.py
  YENİ       : schemas/organization.py
  YENİ       : services/organization_service.py
  YENİ       : api/v1/organizations.py

========================================================
MODELS/ORGANIZATION.PY — ORGANİZASYON MODELLERİ
========================================================

## NEDEN BU DOSYA OLUŞTURULDU?
Platform multi-tenant: farklı şirketler (organizasyonlar) sistemi
kullanır. Her kullanıcının her organizasyonda farklı yetkisi olabilir.
Bu ilişkiyi üç ayrı yapıyla çözdük: OrganizationRole, Organization,
UserOrganizationRole.

## OrganizationRole(str, Enum)

class OrganizationRole(str, Enum):
    OWNER  = "owner"
    ADMIN  = "admin"
    MEMBER = "member"
    VIEWER = "viewer"

  str, Enum kombinasyonu neden?
  → str : DB'ye string olarak yazılır, JSON'a string olarak serialize
          edilir, karşılaştırma kolaylaşır ("owner" == OrganizationRole.OWNER)
  → Enum: geçersiz değer atanması önlenir, IDE autocomplete çalışır
  → plain str kullansaydık "ownr" gibi typo'lar runtime'a kadar fark
    edilmezdi

  Roller neden 4 tane?
  → OWNER  : organizasyonu kuran, siler, her şeye erişir
  → ADMIN  : üye ekler/çıkarır, ayarları değiştirir
  → MEMBER : doküman yükler, chat yapar
  → VIEWER : sadece okur, chat yapamaz
  İleride yetki kontrolü bu rollere göre yapılacak.

## Organization(Base)

__tablename__ = "organizations"

  id: Mapped[int] — primary_key=True, index=True
    → Her organizasyonun benzersiz kimliği
    → index → WHERE id=? sorguları hızlı

  name: Mapped[str] — String(255), nullable=False, index=True
    → Organizasyon adı zorunlu
    → index=True → isme göre arama yapılacak (admin panel, autocomplete)

  created_at / updated_at: DateTime, default=datetime.utcnow
    → Audit trail: ne zaman kuruldu, ne zaman değişti

  members = relationship("UserOrganizationRole",
                          back_populates="organization",
                          cascade="all, delete-orphan")
    → organization.members diyerek tüm üye kayıtlarına erişilir
    → cascade="all, delete-orphan": organizasyon silinince DB'de
      o organizasyona ait tüm UserOrganizationRole satırları da
      otomatik silinir, orphan (sahipsiz) kayıt kalmaz

## UserOrganizationRole(Base)

Kullanıcı ile Organizasyon arasındaki M:N (many-to-many) ilişkiyi
çözen junction (ara) tablo. Her satır "şu kullanıcı şu rolde şu
organizasyona üye" anlamına gelir.

__tablename__ = "user_organization_roles"

  __table_args__ = (UniqueConstraint("user_id", "organization_id",
                                      name="uq_user_organization"),)
    → Bir kullanıcı aynı organizasyona iki kez eklenemez.
    → DB seviyesinde kısıt — uygulama katmanı bypass edilse bile
      DB reddeder.

  user_id: ForeignKey("users.id", ondelete="CASCADE")
    → users tablosuna FK bağı
    → ondelete="CASCADE": kullanıcı silinince bu satırlar da silinir

  organization_id: ForeignKey("organizations.id", ondelete="CASCADE")
    → organizations tablosuna FK bağı
    → ondelete="CASCADE": organizasyon silinince bu satırlar da silinir

  role: Mapped[str] — String(50), default=OrganizationRole.MEMBER.value
    → Rolü string olarak saklar (enum.value: "member" gibi)
    → default=MEMBER: belirtilmezse üye olur, admin/owner elle verilmeli

  created_at: DateTime, default=datetime.utcnow
    → Ne zaman eklendi — raporlama ve audit için

  organization = relationship("Organization", back_populates="members")
    → user_role.organization diyerek Organization nesnesine ulaşılır
    → GET /organizations/me endpoint'inde nested response için kullanılır

  NOT: User modeline back_populates eklenmedi.
    → Fonksiyonel sorun yok ama ileride user_role.user erişimi
      gerekirse User modeline de relationship eklenmeli.

========================================================
MODELS/USER.PY — GÜNCELLEME: organization_roles EKLENDI
========================================================

Değişen tek satır:

  organization_roles = relationship(
      "UserOrganizationRole",
      cascade="all, delete-orphan",
  )

  Neden eklendi?
    → user.organization_roles diyerek kullanıcının tüm
      organizasyon üyeliklerini ORM üzerinden çekmek için

  cascade="all, delete-orphan"
    → Kullanıcı silinince o kullanıcıya ait tüm
      UserOrganizationRole satırları da silinir
    → DB'deki ondelete="CASCADE" ile çift güvence

  back_populates neden yok?
    → UserOrganizationRole'de "user" adlı back relationship
      henüz tanımlanmadı. Tek yönlü. Eklenmesine gerek
      duyulduğunda iki tarafa da eklenecek.

========================================================
MODELS/__INIT__.PY — GÜNCELLEME
========================================================

Önceki hal:
  from app.models.user import User
  __all__ = ["User"]

Yeni hal:
  from app.models.user import User
  from app.models.organization import Organization, UserOrganizationRole
  __all__ = ["User", "Organization", "UserOrganizationRole"]

  Neden eklendi?
    → Alembic autogenerate çalışırken Base.metadata'yı tarar.
      Bir model hiçbir yerden import edilmemişse metadata'ya
      dahil olmaz, migration oluşturulamaz.
    → Yeni modeller buraya eklendikçe Alembic hepsini görür.

========================================================
SCHEMAS/ORGANIZATION.PY — ORGANİZASYON ŞEMALARI
========================================================

## OrganizationCreate
POST ile organizasyon oluşturma isteğinde kullanılır.

  name: str = Field(min_length=2, max_length=255)
    → min_length=2 : tek harflik isimleri reddeder
    → max_length=255: DB kolonuyla uyumlu, aşılamaz

## OrganizationRead
Organizasyon bilgilerini API'den dönerken kullanılır.

  id, name, created_at
  model_config = {"from_attributes": True}
    → ORM nesnesi doğrudan serialize edilebilir

  Neden updated_at yok?
    → API kullanıcısı için gerekli değil, sadece internal.
      İleride admin endpoint'inde eklenebilir.

## UserOrganizationRead
GET /organizations/me endpoint'inin response şeması.
Kullanıcının üye olduğu her organizasyon + rolu döner.

  organization: OrganizationRead
    → İç içe nested schema. UserOrganizationRole ORM nesnesi
      üzerinden organization relationship'i otomatik serialize edilir.

  role: str
    → "owner", "admin", "member", "viewer" değerlerinden biri

  model_config = {"from_attributes": True}
    → Nested relationship'lerin ORM'den okunabilmesi için zorunlu.
      Bu olmadan organization alanı serialize edilemez.

  Örnek response:
    [
      {
        "organization": {"id": 1, "name": "Acme Corp", "created_at": "..."},
        "role": "owner"
      }
    ]

========================================================
SCHEMAS/USER.PY — GÜNCELLEME: organization_name EKLENDI
========================================================

UserCreate'e yeni alan:

  organization_name: str = Field(min_length=2, max_length=255)

  Neden kayıt formunda organizasyon adı isteniyor?
    → Multi-tenant sistemde her kullanıcı bir organizasyona ait olmalı.
      Kullanıcı kayıt olunca aynı anda organizasyon da oluşturulur.
    → Alternatif tasarım: önce kayıt ol, sonra organizasyon kur.
      Bu tasarımda kullanıcı oluşturma ve organizasyon kurma atomik
      (ya hepsi ya hiçbiri) olduğu için tek formda toplandı.

  max_length=255 neden?
    → DB kolonuyla (String(255)) birebir uyumlu.
      DB'de 255 olan alana 300 karakter gönderilirse Pydantic
      API katmanında reddeder, DB'ye kadar gitmez.

  Önceki password max_length değişikliği de bu commit'te:
    password: str = Field(min_length=6, max_length=72)
      → Önceki max 128'di, 72'ye indirildi.
      → Neden 72? bcrypt iç yapısı gereği 72 byte'tan uzun giriş
        kırpılır. 73. karakter ve sonrası bcrypt tarafından yok
        sayılır. max_length=72 bu davranışla bilinçli hizalandı.

========================================================
SERVICES/ORGANIZATION_SERVICE.PY — YENİ DOSYA
========================================================

## NEDEN AYRI BİR SERVICE?
Organization iş mantığı user_service.py'a gömülseydi:
  - Organizasyon işlemleri başka endpoint'lerden çağırılamaz
  - Test edilmesi zorlaşır
  - user_service.py şişer

Ayrı dosyada: her servis tek bir domain'den sorumlu.

## create_organization(db, name) → Organization

  organization = Organization(name=name)
  db.add(organization)
  db.flush()
  return organization

  db.flush() — neden commit değil?
    → Flush: değişikliği SQL'e çevirir ama transaction'ı kapatmaz.
    → Bu fonksiyon transaction yönetmez, çağıran yönetir.
    → user_service.create_user() hem user hem org hem rolü tek bir
      db.commit() ile atomik kaydeder. flush sayesinde org.id alınır
      ama henüz DB'ye yazılmaz. Hata olursa ikisi de geri alınır.

## add_user_to_organization(db, user_id, organization_id, role) → UserOrganizationRole

  user_role = UserOrganizationRole(
      user_id=user_id,
      organization_id=organization_id,
      role=role.value,   ← enum değil string: "owner", "member"...
  )
  db.add(user_role)
  db.flush()
  return user_role

  role=OrganizationRole.MEMBER default olarak
    → Belirtilmezse üye olur. Şu an yalnızca OWNER ile çağrılıyor
      ama gelecekte davet sistemi MEMBER ile çağıracak.

  role.value neden?
    → DB kolonu String(50), Enum objesi değil string bekler.
      role.value → "owner", "admin" gibi string döner.

## get_user_organizations(db, user_id) → list[UserOrganizationRole]

  db.query(UserOrganizationRole)
    .filter(UserOrganizationRole.user_id == user_id)
    .all()

  → GET /organizations/me endpoint'inin tek sorgusu.
  → ORM üzerinden organization relationship'i lazy load olarak
    çekilir. UserOrganizationRead serialize ederken otomatik JOIN.

========================================================
SERVICES/USER_SERVICE.PY — GÜNCELLEME
========================================================

## create_user() İÇİNDEKİ DEĞİŞİKLİKLER

Önceki hal:
  db.add(user)
  db.commit()
  db.refresh(user)

Yeni hal:
  db.add(user)
  db.flush()          ← commit yerine flush

  organization = create_organization(db, name=user_in.organization_name)
  add_user_to_organization(
      db,
      user_id=user.id,
      organization_id=organization.id,
      role=OrganizationRole.OWNER,
  )

  db.commit()         ← tek commit, her şeyi kapsar
  db.refresh(user)

  Neden flush → commit değil commit → commit?

  db.flush() sonrası:
    → user.id artık None değil (DB sequence ID üretildi)
    → Ama transaction hala açık, henüz commit edilmedi

  Atomicity:
    → Kullanıcı oluştu ama organizasyon oluşturma başarısız olduysa
      her iki kayıt da geri alınır.
    → Yarım kullanıcı (organizasyonsuz) DB'de KALMAZ.
    → İki ayrı commit kullansaydık: user commit'lendi, org exception
      fırlattı → DB'de organizasyonsuz kullanıcı kalırdı. Hatalı durum.

  Yeni import'lar eklendi:
    from app.models.organization import OrganizationRole
    from app.services.organization_service import (
        add_user_to_organization,
        create_organization,
    )

## authenticate_user() — HATIRLATMA (bu commit'te yazıldı)

  Önceki notlarda detaylandırıldı. Kısa özet:
  → Email + şifre doğrulama. User veya None döner.
  → "email yok" ve "şifre yanlış" ayırt edilmez.
    User Enumeration saldırısını önlemek için.

========================================================
API/V1/ORGANIZATIONS.PY — YENİ ENDPOINT
========================================================

## GET /organizations/me

router = APIRouter(prefix="/organizations", tags=["Organizations"])

@router.get("/me", response_model=list[UserOrganizationRead])
def read_my_organizations(
    db: Session = Depends(get_db),
    current_user = Depends(get_current_user),
):
    return get_user_organizations(db, user_id=current_user.id)

  response_model=list[UserOrganizationRead]
    → Birden fazla organizasyon üyeliği olabilir, liste döner.
    → Her eleman: organizasyon bilgisi + rol.

  Depends(get_current_user)
    → users.py'deki dependency kullanılır.
    → Token doğrulama bu endpoint'e tek satır eklenerek sağlandı.
    → Dependency yeniden kullanımının avantajı burada görülür.

  Neden current_user'ı parametre olarak almak daha iyi?
    → Endpoint içinde token parse etmek, DB sorgusu çekmek
      gibi işler tekrarlanmaz.
    → Test ederken current_user mock'lanabilir.

  OLUŞAN URL: /api/v1/organizations/me

========================================================
API/V1/ROUTER.PY — GÜNCELLEME
========================================================

Önceki hal:
  from app.api.v1 import auth, users
  api_router.include_router(auth.router)
  api_router.include_router(users.router)

Yeni hal:
  from app.api.v1 import auth, users, organizations
  api_router.include_router(auth.router)
  api_router.include_router(users.router)
  api_router.include_router(organizations.router)

  Neden sadece bu iki satır?
    → organizations.router kendi prefix'ini (/organizations)
      kendi dosyasında tanımlıyor. Router burada sadece birleştiriliyor.

GÜNCEL URL TABLOSu:
  /api/v1/auth/register          → POST
  /api/v1/auth/login             → POST
  /api/v1/users/me               → GET (token gerekli)
  /api/v1/organizations/me       → GET (token gerekli)

========================================================
BU KOMMİTİN BÜYÜK RESMİ — TÜM AKIŞ
========================================================

KAYIT (POST /auth/register):
  UserCreate (email, password, organization_name)
      ↓
  Email kayıtlı mı? → 400
      ↓
  create_user()
    ├── get_password_hash()       → bcrypt hash
    ├── User ORM nesnesi oluştur
    ├── db.flush()                → user.id alındı, henüz commit yok
    ├── create_organization()     → db.flush()
    ├── add_user_to_organization(role=OWNER) → db.flush()
    └── db.commit()               → üçü birden tek transaction
      ↓
  UserRead filtrele → 201

GİRİŞ (POST /auth/login):
  authenticate_user() → user.id
      ↓
  create_access_token(user.id) → JWT
      ↓
  Token → {"access_token": "...", "token_type": "bearer"}

KENDİ ORGANİZASYONLARIM (GET /organizations/me):
  Authorization: Bearer <token>
      ↓
  get_current_user() → token doğrula → User nesnesi
      ↓
  get_user_organizations(user_id)
      ↓
  list[UserOrganizationRead] → nested JSON response

========================================================

========================================================
COMMIT: "Add document upload and listing endpoints"
— DOKÜMAN YÖNETİM KATMANININ EKLENMESİ
========================================================

Bu commit ile sistemin temel işlevi olan dosya yükleme
altyapısı kuruldu. Kullanıcılar kendi organizasyonlarına
dosya yükleyebilir, listeleyebilir ve tek tek sorgulayabilir.

Etkilenen dosyalar (migration hariç):
  GÜNCELLEME : core/config.py
  GÜNCELLEME : models/__init__.py
  GÜNCELLEME : api/v1/router.py
  YENİ       : models/document.py
  YENİ       : schemas/document.py
  YENİ       : services/document_service.py
  YENİ       : api/v1/documents.py
  YENİ       : alembic/versions/7aac9eca3357_add_documents_table.py

========================================================
CORE/CONFIG.PY — GÜNCELLEME: UPLOAD_DIR EKLENDİ
========================================================

Değişen tek satır:
  UPLOAD_DIR: str = "uploads"

  Neden eklendi?
    → Yüklenen dosyaların hangi klasöre kaydedileceği
      sabit string yerine config üzerinden yönetilmeli.
    → .env dosyasında UPLOAD_DIR=/data/uploads denerek
      prod/dev ortamlarında farklı yollar kullanılabilir.
    → Tüm servis kodları settings.UPLOAD_DIR ile bu değeri
      okur, hiçbir yerde hardcoded path kalmaz.
    → Güvenlik: farklı ortamlarda farklı disk bölümlerine
      yönlendirme, container mount noktaları için esneklik.

========================================================
MODELS/DOCUMENT.PY — YENİ: DOKÜMAN MODELİ
========================================================

## DocumentStatus(str, Enum)

class DocumentStatus(str, Enum):
    UPLOADED   = "uploaded"
    PROCESSING = "processing"
    COMPLETED  = "completed"
    FAILED     = "failed"

  str, Enum kombinasyonu neden?
    → str   : DB'ye ve JSON'a string olarak yazılır.
    → Enum  : Geçersiz durum ataması önlenir.
    → Durum geçişi:
        Yüklendi → Celery kuyruğa aldı → tamamlandı/hata
        UPLOADED → PROCESSING → COMPLETED veya FAILED
    → Celery worker'lar doküman işlerken bu durumu günceller.
      RAG pipeline'ın hangi aşamada olduğunu izlemek için.

## Document(Base) — Kolon Analizi

__tablename__ = "documents"

  id: Integer — primary_key=True, index=True
    → Her dokümanın benzersiz ID'si.

  organization_id: ForeignKey("organizations.id", ondelete="CASCADE")
    → Doküman hangi organizasyona ait.
    → ondelete="CASCADE": Organizasyon silinince o organizasyonun
      tüm dokümanları da otomatik silinir, orphan kayıt kalmaz.
    → index=True: "Bu organizasyonun dokümanlarını getir" sorgusu
      çok sık çalışacak, index kritik performans için.

  uploaded_by_user_id: ForeignKey("users.id", ondelete="SET NULL")
    → Dosyayı kim yükledi.
    → ondelete="SET NULL" (CASCADE değil): Kullanıcı silinse bile
      doküman korunur, sadece bu alan NULL olur.
    → nullable=True: Sistem/otomatik yükleme ihtimali bırakıldı.
    → Belge tarihçesi kullanıcı hesabından bağımsız tutulur.

  original_file_name: String(255), nullable=False
    → Kullanıcının yüklediği gerçek dosya adı (örn: "sözleşme.pdf").
    → Kullanıcıya gösterilir, indirme ekranında kullanılır.

  stored_file_name: String(255), nullable=False
    → Diskte saklanan UUID'li isim (örn: "a3f7...pdf").
    → original_file_name asla disk yolu olarak kullanılmaz.
    → Güvenlik: "../../../etc/passwd" gibi path traversal
      saldırıları önlenir. Kullanıcı girdisi disk'e yazmaz.
    → Aynı isimde iki dosya çakışmaz, her dosya benzersiz.

  file_path: String(500), nullable=False
    → Dosyanın diskteki tam yolu.
    → Celery worker dokümanı işlerken bu path'i kullanır.

  content_type: String(100), nullable=True
    → MIME tipi (application/pdf, application/vnd.openxmlformats...)
    → Dosya indirme endpoint'inde Content-Type header'ı için.

  file_size: Integer, nullable=True
    → Bayt cinsinden dosya büyüklüğü.
    → Kullanım kotası takibi için. (İleride: organizasyon
      başına maksimum depolama limiti)

  status: String(50), default=DocumentStatus.UPLOADED.value
    → RAG pipeline durumu. UPLOADED ile başlar.
    → index=True: "İşlenmemiş dosyaları getir" gibi sorgular için.

  created_at / updated_at: DateTime, default=datetime.utcnow
    → updated_at onupdate=datetime.utcnow: Durum her
      değiştiğinde (PROCESSING, COMPLETED...) otomatik güncellenir.

  organization = relationship("Organization")
  uploaded_by  = relationship("User")
    → ORM üzerinden ilişkili nesneye erişim.
    → back_populates eklenmedi: Henüz ters yönden erişim
      gerekmediği için tek yönlü bırakıldı.

========================================================
MODELS/__INIT__.PY — GÜNCELLEME
========================================================

Önceki hal:
  from app.models.user import User
  from app.models.organization import Organization, UserOrganizationRole
  __all__ = ["User", "Organization", "UserOrganizationRole"]

Yeni hal:
  from app.models.document import Document eklendi
  __all__'a "Document" eklendi

  Neden gerekli?
    → Alembic, Base.metadata'yı tarar. Document hiçbir yerden
      import edilmemişse migration otomatik üretilemez.
    → Bu satır Alembic'in "documents" tablosunu görmesini sağlar.

========================================================
SCHEMAS/DOCUMENT.PY — YENİ: DOKÜMAN READ ŞEMASI
========================================================

class DocumentRead(BaseModel):
    id, organization_id, uploaded_by_user_id
    original_file_name, stored_file_name, file_path
    content_type, file_size, status, created_at

  model_config = {"from_attributes": True}
    → SQLAlchemy ORM nesnesinden doğrudan okur.
    → db.query(Document).first() → DocumentRead.model_validate(doc)

  Tasarım kararları:
    → updated_at yok: Client'ın buna ihtiyacı olmadığı düşünüldü.
    → file_path var: İleride indirme endpoint'i bu path'i kullanır.
    → stored_file_name var: Admin debug için faydalı.
    → Ayrı bir DocumentCreate şeması yazılmadı çünkü dosya yükleme
      JSON body ile değil multipart/form-data ile gelir.
      Pydantic şeması değil, UploadFile parametresi kullanılır.

========================================================
SERVICES/DOCUMENT_SERVICE.PY — YENİ: DOKÜMAN SERVİS KATMANI
========================================================

## validate_file_extension(filename) → None | ValueError

ALLOWED_EXTENSIONS = {".pdf", ".docx", ".xlsx", ".xls", ".txt"}

  Path(filename).suffix.lower() kullanımı neden?
    → "RAPOR.PDF" → ".pdf" (büyük/küçük harf normalize edilir)
    → "dosya.tar.gz" → ".gz" (çift uzantılarda son kısım alınır)
    → Güvenlik: extension whitelist (izin verilenler listesi).
      Blacklist (yasaklılar listesi) yerine whitelist yaklaşımı
      daha güvenli: bilinmeyen her format otomatik reddedilir.
    → .exe, .sh, .py gibi tehlikeli dosyalar sunucuya alınmaz.

## create_upload_directory() → str

  os.makedirs(upload_dir, exist_ok=True)
    → Klasör yoksa oluşturur, varsa hata fırlatmaz.
    → Her yükleme isteğinde kontrol edilir.

## generate_stored_file_name(original_file_name) → str

  uuid.uuid4() + extension
    → UUID v4: kriptografik olarak tahmin edilemez benzersiz ID.
    → Kullanıcı adı disk yoluna hiç karışmaz.
    → Güvenlik: "../../etc/passwd.pdf" gibi isimler diskte zararsız
      bir UUID'ye dönüşür. Path traversal saldırısı engellenir.

## save_upload_file(upload_file, stored_file_path) → int

  Chunk'lı okuma: 1 MB'lık parçalarda okur ve yazar.
    → Neden? 100 MB PDF doğrudan belleğe alınsaydı tüm dosya
      RAM'de tutulurdu. Büyük dosyalarda bellek patlaması.
    → 1 MB chunk: bellek sabit, dosya ne kadar büyük olursa olsun.
    → total_size sayacı: her chunk'ın boyutu toplanarak
      gerçek dosya boyutu hesaplanır (content-length header'ına
      güvenilmez, manipüle edilebilir).
  Return: int → bayt cinsinden toplam dosya boyutu.

## create_document_record(db, organization_id, uploaded_by_user_id,
                           upload_file) → Document

  Tüm adımlar sırasıyla:
  1. validate_file_extension() → izin verilen uzantı mı?
  2. create_upload_directory()  → klasör var mı, yoksa oluştur
  3. generate_stored_file_name()→ UUID'li güvenli isim üret
  4. os.path.join(upload_dir, stored_file_name) → tam yol
  5. save_upload_file() → dosyayı diske yaz, boyutu al
  6. Document(...) ORM nesnesi oluştur
  7. db.add / db.commit / db.refresh
  8. return document

  Önemli: Dosya önce diske yazılır, sonra DB'ye kaydedilir.
  Tersine yapılsaydı DB kaydı başarılı ama disk yazımı başarısız
  olabilirdi. Şu an DB başarılı ama disk başarısız olursa DB'de
  dosyasız kayıt kalır — bu bilinçli kabul edilmiş teknik borç,
  ileride transactional dosya yönetimi veya Celery ile çözülecek.

## get_documents_by_organization(db, organization_id) → list[Document]

  .order_by(Document.created_at.desc())
    → En yeni dosya önce gelir. Listeleme UI için doğal sıra.

## get_document_by_id_and_organization(db, document_id,
                                        organization_id) → Document | None

  İKİ filtre birlikte kullanılır:
    Document.id == document_id
    Document.organization_id == organization_id
  Neden?
    → Sadece ID filtresi kullansaydık başka organizasyonun
      dokümanına ID biliniyorsa erişilebilirdi.
    → İki filtre: sadece kendi organizasyonundaki dosyaya erişilir.
    → Tenant isolation (kiracı izolasyonu) sağlanır.

========================================================
API/V1/DOCUMENTS.PY — YENİ: DOKÜMAN API ENDPOINT'LERİ
========================================================

## get_current_user_primary_organization_id(db, user_id) → int

  Tüm endpoint'lerde tekrar eden bir işlem:
  "Bu kullanıcının birincil organizasyonu hangisi?"
  Yardımcı fonksiyon olarak çıkarıldı.

  Neden [0].organization_id?
    → Bir kullanıcı birden fazla organizasyona üye olabilir.
    → Şimdilik basit tutuldu: ilk organizasyon kullanılır.
    → İleride: hangi organizasyon bağlamında işlem yapılacağını
      request header veya query param ile belirleyen yapı kurulacak.
    → Organizasyonu yoksa 400: kullanıcı sistemi kullanamaz.

## POST /documents/upload

  @router.post("/upload", response_model=DocumentRead, status_code=201)
    → status_code=201: Kaynak oluşturuldu.
    → response_model=DocumentRead: ORM nesnesi şemayla filtrele.

  file: UploadFile = File(...)
    → multipart/form-data ile dosya alır.
    → ... (Ellipsis): alan zorunlu, gönderilmezse 422 döner.
    → UploadFile: dosyayı RAM'e almaz, stream olarak okur.

  ValueError yakalanır → HTTPException 400'e çevrilir.
    → validate_file_extension geçersiz uzantıda ValueError fırlatır.
    → Service katmanı HTTP bilmez, sadece ValueError fırlatır.
    → API katmanı bunu 400 Bad Request'e çevirir. Katman ayrımı.

## GET /documents

  Giriş yapmış kullanıcının organizasyonuna ait tüm dokümanları listeler.
  Tenant isolation: organization_id ile filtrelenir, başka
  organizasyonun dosyaları hiçbir zaman bu listeye girmez.

## GET /documents/{document_id}

  Tek doküman detayı. Document bulunamazsa 404 döner.
  get_document_by_id_and_organization: iki filtre (id + org_id)
  ile başka organizasyonun dosyasına erişim engellenir.

========================================================
API/V1/ROUTER.PY — GÜNCELLEME: DOCUMENTS ROUTER EKLENDİ
========================================================

Önceki hal:
  from app.api.v1 import auth, users, organizations
  api_router.include_router(auth.router)
  api_router.include_router(users.router)
  api_router.include_router(organizations.router)

Yeni hal:
  from app.api.v1 import auth, users, organizations, documents
  api_router.include_router(documents.router)

Oluşan yeni URL'ler:
  POST /api/v1/documents/upload  → dosya yükle
  GET  /api/v1/documents         → listeleme
  GET  /api/v1/documents/{id}    → tek doküman

ROUTER HİYERARŞİSİ GÜNCELLENDİ:
  main.py → api_router (/api/v1)
                ├── auth.router          → /api/v1/auth/...
                ├── users.router         → /api/v1/users/...
                ├── organizations.router → /api/v1/organizations/...
                └── documents.router     → /api/v1/documents/...

========================================================
MİGRASYON: 7aac9eca3357_add_documents_table.py
========================================================

  Revision    : 7aac9eca3357
  Revises     : d2294bf51c25 (organizasyonlar migration'ı)
  Create Date : 2026-05-15 17:18:18

  upgrade() ile oluşturulan şey:
    - documents tablosu: tüm kolonlar
    - organization_id FK → organizations.id (ondelete=CASCADE)
    - uploaded_by_user_id FK → users.id (ondelete=SET NULL)
    - 4 adet index:
        ix_documents_id                → PK sorguları
        ix_documents_organization_id   → "bu org'un dosyaları" sorgusu
        ix_documents_uploaded_by_user_id → "bu user'ın dosyaları"
        ix_documents_status            → "işlenmemiş dosyaları getir"
                                          (Celery worker için kritik)

  downgrade():
    → 4 index drop, sonra tablo drop.
    → Index önce drop edilir: FK/index varken tablo droplanamaz.

  Komutlar:
    alembic revision --autogenerate -m "add documents table"
    alembic upgrade head

========================================================
TÜM DOKÜMAN YÜKLEME AKIŞI — BÜYÜK RESİM
========================================================

POST /api/v1/documents/upload + Authorization: Bearer <token>
        ↓
get_current_user() → JWT doğrulandı, user nesnesi alındı
        ↓
get_current_user_primary_organization_id() → org_id belirlendi
        ↓
validate_file_extension() → .pdf/.docx/... onaylandı
        ↓
generate_stored_file_name() → UUID isim üretildi
        ↓
save_upload_file() → 1 MB chunk'larla diske yazıldı
        ↓
Document ORM nesnesi → db.add/commit/refresh → DB'ye kaydedildi
        ↓
DocumentRead şeması → 201 response
        ↓
(İleride) Celery → status=PROCESSING → chunk → embed → vector DB
                → status=COMPLETED

GÜVENLİK ÖZETİ:
  - Extension whitelist: sadece bilinen güvenli tipler
  - UUID stored name: kullanıcı girdisi disk yoluna karışmaz
  - Tenant isolation: her sorgu organization_id ile filtrelenir
  - Kimlik doğrulama: tüm endpoint'ler get_current_user gerektirir
  - Dosya boyutu: gerçek byte sayımı (header'a güvenilmez)

========================================================

========================================================
COMMIT: "Add document parsing, chunking and processing pipeline"
— DOKÜMAN İŞLEME PIPELINE'ININ EKLENMESİ
========================================================

Bu commit ile yüklenen dosyalar artık otomatik olarak parse edilir,
metin parçalara (chunk) bölünür ve her chunk veritabanına kaydedilir.
RAG pipeline'ının ilk kritik aşaması tamamlandı.

Etkilenen dosyalar (migration hariç):
  GÜNCELLEME : models/__init__.py
  GÜNCELLEME : models/document.py  (chunks relationship eklendi)
  GÜNCELLEME : api/v1/documents.py (işleme + chunks endpoint eklendi)
  YENİ       : models/document_chunk.py
  YENİ       : schemas/document_chunk.py
  YENİ       : services/parser_service.py
  YENİ       : services/chunking_service.py
  YENİ       : services/document_processing_service.py

========================================================
MODELS/DOCUMENT_CHUNK.PY — YENİ: CHUNK MODELİ
========================================================

DocumentChunk(Base) — "document_chunks" tablosunu tanımlar.
Her satır bir dokümanın metin parçasını temsil eder.

  id: Integer — primary_key=True, index=True
    → Her chunk'ın benzersiz ID'si.

  document_id: ForeignKey("documents.id", ondelete="CASCADE")
    → Hangi dokümana ait olduğu.
    → ondelete="CASCADE": Doküman silinince o dokümana ait tüm
      chunk'lar otomatik silinir.
    → index=True: "Bu dokümanın chunk'larını getir" sorgusu kritik,
      index olmadan N chunk için full table scan olur.

  organization_id: ForeignKey("organizations.id", ondelete="CASCADE")
    → Organizasyon bazlı erişim kontrolü için doğrudan tutulur.
    → Neden document üzerinden değil doğrudan burada?
      JOIN yazmadan "bu organizasyonun chunk'ları" filtrelenebilsin.
      İleride vector search sorguları bu alana göre filtre uygular.
    → ondelete="CASCADE": Organizasyon silinince chunk'lar da silinir.

  chunk_index: Integer, nullable=False, index=True
    → Chunk'ın belgedeki sırası (0, 1, 2...).
    → Sıralı listeleme ve context window oluşturma için gerekli.
    → index=True: ORDER BY chunk_index sorgularına hız sağlar.

  content: Text, nullable=False
    → Chunk'ın gerçek metin içeriği.
    → Text tipi seçildi çünkü String(N) için N belirsiz.
      Chunk boyutu 1200 karakter ama UTF-8 multibyte karakter
      ve overlap varken safe olmak için Text kullanıldı.

  page_number: Integer, nullable=True
    → PDF için sayfa numarası. PDF dışı dosyalarda NULL.
    → Kaynak gösterme (citation): "Bu bilgi sayfa 12'de."

  sheet_name: String(255), nullable=True
    → Excel için sayfa adı. Excel dışı dosyalarda NULL.
    → Kaynak gösterme: "Bu bilgi 'Bütçe 2024' sayfasında."

  source_type: String(50), nullable=True
    → Dosya türü: "pdf", "docx", "xlsx", "txt".
    → Farklı kaynak türlerini UI'da ayırt etmek için.
    → Neden nullable? Gelecekte başka kaynaklardan (URL, API)
      gelen chunk'lar için NULL bırakma esnekliği.

  created_at: DateTime, default=datetime.utcnow
    → Chunk ne zaman oluşturuldu.

  document = relationship("Document", back_populates="chunks")
    → chunk.document ile Document nesnesine ulaşılır.
    → back_populates="chunks": iki yönlü ilişki (document.chunks
      ve chunk.document birbirini bilir).

========================================================
MODELS/DOCUMENT.PY — GÜNCELLEME: chunks RELATIONSHIP EKLENDİ
========================================================

Document modeline tek yeni satır:

  chunks = relationship(
      "DocumentChunk",
      back_populates="document",
      cascade="all, delete-orphan",
  )

  Ne sağlar?
    → document.chunks yazarak o dokümana ait tüm chunk'lara
      ORM üzerinden erişilir. JOIN yazmak gerekmez.

  cascade="all, delete-orphan" neden?
    → Doküman silinince SQLAlchemy session seviyesinde de
      chunk'ları temizler. DB'deki ondelete="CASCADE" ile
      çift güvence sağlanır: DB ve ORM her ikisi de temizler.
    → delete-orphan: document.chunks listesinden bir chunk
      çıkarılırsa (pop/remove) ORM onu otomatik siler.

========================================================
MODELS/__INIT__.PY — GÜNCELLEME
========================================================

Önceki __all__:
  ["User", "Organization", "UserOrganizationRole", "Document"]

Yeni __all__:
  ["User", "Organization", "UserOrganizationRole",
   "Document", "DocumentChunk"]

SORUN: DocumentChunk __all__'a eklendi ama import satırı
       eksik kaldı (from app.models.document_chunk import DocumentChunk).
       Bu durum "from app.models import DocumentChunk" çalışmaz.
       Alembic autogenerate çalışıyorsa document_chunk.py zaten
       document_processing_service.py üzerinden import edildiği için
       şimdilik sorun çıkmayabilir, ama __init__.py'a import eklenmeli.

========================================================
SCHEMAS/DOCUMENT_CHUNK.PY — YENİ: CHUNK READ ŞEMASI
========================================================

class DocumentChunkRead(BaseModel):
    id, document_id, organization_id
    chunk_index, content
    page_number, sheet_name, source_type
    created_at

  model_config = {"from_attributes": True}
    → ORM nesnesinden doğrudan serialize edilir.
    → GET /{document_id}/chunks endpoint'inin response şeması.

  Neden Create şeması yok?
    → Chunk'lar hiçbir zaman API üzerinden doğrudan oluşturulmaz.
      Sadece document_processing_service.py içeride yazar.
      Dışarıdan chunk yazmak güvenlik açığı olurdu.

========================================================
SERVICES/PARSER_SERVICE.PY — YENİ: DOSYA AYRIŞTIRICISI
========================================================

ParsedSection = dict[str, Any]
  → Her ayrıştırma sonucu: {"text": ..., "page_number": ...,
                             "sheet_name": ..., "source_type": ...}

## parse_txt(file_path) → list[ParsedSection]
  Tüm dosya tek bir section olarak döner.
  encoding="utf-8", errors="ignore": Bozuk karakterler atlanır,
  dosya yükleme başarısız olmaz.

## parse_pdf(file_path) → list[ParsedSection]
  PdfReader ile her sayfa ayrı section olur.
  page_number = sayfa numarası (1'den başlar).
  Boş sayfalar (text.strip() == "") atlanır.
  Neden sayfa bazlı?
    → PDF'in hangi sayfasından geldiği chunk'ta kaydedilir.
    → RAG sonuçlarında kaynak: "sayfa 5'te bu konudan bahsedilmekte."

## parse_docx(file_path) → list[ParsedSection]
  python-docx ile tüm paragraflar okunur, boşlar atlanır.
  Tek bir text string'e birleştirilir (join).
  Tüm dosya tek section → pdf gibi bölünmez çünkü docx'ta
  sayfa numarası güvenilir şekilde çıkarılamaz.
  page_number=None, sheet_name=None, source_type="docx"

## parse_xlsx(file_path) → list[ParsedSection]
  openpyxl ile her worksheet ayrı section olur.
  sheet.title → sheet_name alanına yazılır.
  Her satırın hücreleri " | " ile birleştirilir:
    ["Ahmet", "Satış", "150000"] → "Ahmet | Satış | 150000"
  Boş hücreler atlanır. Boş satırlar atlanır. Boş sheet atlanır.
  read_only=True: büyük Excel dosyalarında bellek tasarrufu.
  data_only=True: formül yerine hesaplanmış değer okunur.

## parse_document(file_path) → list[ParsedSection]
  Dispatch fonksiyonu. Extension'a göre doğru parser'ı çağırır.
  Bilinmeyen extension → ValueError fırlatır.
  Güvenlik: whitelist mantığı — sadece bilinen tipler işlenir.
  Yeni format eklemek: sadece buraya elif eklenir, başka kod değişmez.

========================================================
SERVICES/CHUNKING_SERVICE.PY — YENİ: METİN BÖLÜCÜ
========================================================

## split_text_into_chunks(text, chunk_size=1200, chunk_overlap=200)
   → list[str]

  cleaned_text = " ".join(text.split())
    → Çoklu boşluklar, tab, newline tek boşluğa indirilir.
    → Tutarsız whitespace chunk sınırlarında tahmin edilemez
      davranışa yol açar. Normalize edilince chunk boyutları
      daha tutarlı olur.

  Sliding window algoritması:
    start = 0
    while start < text_length:
        end   = start + chunk_size       → chunk sonu
        chunk = cleaned_text[start:end]  → alınan parça
        start = end - chunk_overlap      → sonraki başlangıç

  chunk_overlap=200 neden?
    → Bir bilgi iki chunk arasına denk gelirse hiçbirinde
      tam olarak geçmez → LLM context'i anlayamaz.
    → 200 karakter overlap: her chunk'ın başı bir öncekinin
      sonuyla örtüşür. Bağlam kaybolmaz.
    → RAG kalitesi için kritik tasarım kararı.

## create_chunks_from_sections(sections, chunk_size=1200,
                                chunk_overlap=200)
   → list[dict[str, Any]]

  Her section için split_text_into_chunks çalışır.
  Global chunk_index korunur (section başında sıfırlanmaz).
  Her chunk dict'i:
    {"chunk_index": N, "content": "...",
     "page_number": ..., "sheet_name": ..., "source_type": ...}

  page_number ve sheet_name section'dan kopyalanır.
    → PDF'in 3. sayfasından gelen tüm chunk'lar page_number=3 taşır.
    → Excel'in "Bütçe" sheet'inden gelen chunk'lar sheet_name="Bütçe".

========================================================
SERVICES/DOCUMENT_PROCESSING_SERVICE.PY — YENİ: İŞLEM ORKESTRATÖRü
========================================================

process_document(db, document_id) → Document

Bu fonksiyon tüm pipeline'ı yönetir:
parse → chunk → DB'ye yaz → status güncelle

## Akış Detayı:

1. document = db.query(Document).filter(id == document_id).first()
   → Doküman yoksa ValueError fırlatılır (API 400'e çevirir).

2. document.status = PROCESSING → db.commit()
   → İşlem başladı sinyali. Uzun süren dosyalarda UI bu durumu gösterir.
   → Neden hemen commit? İşlem çökse bile PROCESSING durumu kalır,
     monitör/admin "takılı kalan" işlemleri görebilir.

3. sections = parse_document(file_path)
   → Parser service dosyayı okur, section listesi döner.

4. chunks = create_chunks_from_sections(sections)
   → Chunking service section'ları chunk'lara böler.

5. Mevcut chunk'ları sil:
   existing_chunks = db.query(DocumentChunk).filter(document_id).all()
   for chunk in existing_chunks: db.delete(chunk)
   db.flush()
   → Neden? Doküman yeniden işlenirse (reprocess) eski chunk'lar
     temizlenir, çift kayıt oluşmaz.
   → db.flush(): silmeler SQL'e çevrilir ama commit edilmez.
     Yeni chunk'lar eklenmeden önce çakışma riski ortadan kalkar.

6. Yeni chunk'ları ekle:
   for chunk in chunks:
       document_chunk = DocumentChunk(document_id, organization_id,
                                       chunk_index, content, ...)
       db.add(document_chunk)

7. document.status = COMPLETED → db.commit() → db.refresh()
   → Tüm chunk'lar ve durum tek commit ile yazılır (atomik).

## Hata Yönetimi (except Exception):

   db.rollback()
   → Yarım yazılan chunk'lar geri alınır.

   document yeniden sorgulanır:
   → Rollback sonrası eski nesne geçersiz (detached state).
     Yeni sorgu ile taze nesne alınır.

   document.status = FAILED → db.commit()
   → Hata durumu DB'ye yazılır.

   raise
   → Hata üst katmana iletilir, API 500 veya 400 döner.

  Neden rollback sonrası yeni sorgu?
    → db.rollback() sonrası session'daki ORM nesneleri
      "detached" olur. Onlar üzerinden işlem yapılmaya
      çalışılırsa "Instance is not bound to a Session" hatası alınır.
      Yeni db.query() ile taze nesne almak zorunlu.

========================================================
API/V1/DOCUMENTS.PY — GÜNCELLEME: İŞLEME VE CHUNK LİSTELEME
========================================================

## POST /documents/upload — DEĞİŞİKLİK

Önceki hal:
  document = create_document_record(...)
  return document   ← sadece DB kaydı döndürülüyordu

Yeni hal:
  document = create_document_record(...)
  processed_document = process_document(db=db, document_id=document.id)
  return processed_document

  Ne değişti?
    → Dosya yüklenir yüklenmez parse + chunk pipeline'ı
      senkron olarak çalışır.
    → Sonuç: status=COMPLETED olan (veya FAILED olan) doküman döner.
    → Neden senkron? Şu an Celery kuyruk sistemi henüz entegre
      değil. İleride process_document Celery task'ına taşınacak.
      O aşamada upload endpoint sadece UPLOADED döner, Celery
      arka planda PROCESSING → COMPLETED/FAILED yapar.

  ValueError yakalanma kapsamı genişledi:
    → create_document_record'dan gelen ValueError (geçersiz uzantı)
    → process_document'ten gelen ValueError (doküman bulunamadı)
    → İkisi de HTTPException 400'e çevrilir.

## GET /documents/{document_id}/chunks — YENİ ENDPOINT

  @router.get("/{document_id}/chunks",
              response_model=list[DocumentChunkRead])

  Akış:
  1. get_current_user_primary_organization_id() → org_id
  2. get_document_by_id_and_organization() → doküman var mı + tenant check
     → 404 döner doküman yoksa
  3. db.query(DocumentChunk)
       .filter(document_id == document.id)
       .order_by(chunk_index.asc())
       .all()

  Neden önce document sorgulanır, doğrudan chunk'a gidilmez?
    → Tenant isolation: başka organizasyonun dokümanına ait
      chunk_id'leri bilerek /chunks endpoint'i çağırılamaz.
      Document sorgusu organization_id ile filtreler.
    → Doküman yoksa 404, varsa chunk'lar döner.

  order_by(chunk_index.asc())
    → Chunk'lar belgedeki sıraya göre gelir.
    → LLM'e context verilirken doğal okuma sırası korunur.

  Oluşan URL: GET /api/v1/documents/{document_id}/chunks

========================================================
MİGRASYONLAR — DOCUMENT_CHUNKS TABLOSU
========================================================

Bu süreçte birden fazla migration denemesi yapıldı:

  47eb180bb248_add_document_chunks_table.py  → ilk deneme
  bbf5718ec7bd_add_document_chunks_table.py  → ikinci deneme
  af7df02fecaa_fix_missing_document_chunks_table.py → düzeltme

  Nedenler:
    → Alembic revizyon zinciri karıştı (multiple heads sorunu).
    → Bazı denemeler merge branch gerektirdi.
    → Sonunda document_chunks tablosu başarıyla oluşturuldu.

  Son migration'da oluşturulan yapı:
    - document_chunks tablosu
    - Kolonlar: id, document_id, organization_id, chunk_index,
                content, page_number, sheet_name, source_type,
                created_at
    - FK: document_id → documents.id (ondelete=CASCADE)
    - FK: organization_id → organizations.id (ondelete=CASCADE)
    - Index'ler:
        ix_document_chunks_id
        ix_document_chunks_document_id     → chunk listeleme
        ix_document_chunks_organization_id → org bazlı filtre
        ix_document_chunks_chunk_index     → sıralama

========================================================
TÜM PIPELINE AKIŞI — BÜYÜK RESİM (GÜNCEL)
========================================================

POST /api/v1/documents/upload + Bearer <token>
        ↓
get_current_user() → kullanıcı doğrulandı
        ↓
get_current_user_primary_organization_id() → org_id
        ↓
validate_file_extension() → whitelist kontrolü
        ↓
save_upload_file() → 1 MB chunk'larla diske yazıldı
        ↓
Document kaydı DB'ye eklendi (status=UPLOADED)
        ↓
process_document() çağrıldı:
  ├── status = PROCESSING → commit
  ├── parse_document() → sections (PDF: sayfa başına, Excel: sheet başına)
  ├── create_chunks_from_sections() → chunk dict listesi
  ├── Eski chunk'lar silindi (flush)
  ├── Yeni DocumentChunk nesneleri eklendi
  └── status = COMPLETED → commit
        ↓
DocumentRead şeması → 201 response (status=COMPLETED)

HATA DURUMU:
  process_document() hata fırlatırsa:
  → db.rollback() (chunk'lar geri alındı)
  → status = FAILED → commit
  → ValueError → API 400

GET /api/v1/documents/{id}/chunks + Bearer <token>
        ↓
Tenant isolation kontrolü (org_id eşleşmeli)
        ↓
list[DocumentChunkRead] → sıralı chunk'lar

========================================================
SONRAKİ ADIMLAR (Planlanmış)
========================================================

  1. Embedding çıkarma:
     Her chunk.content için OpenAI/local model ile vector üret,
     vector DB'ye (Qdrant veya pgVector) kaydet.

  2. Celery entegrasyonu:
     process_document() senkron çalışıyor.
     Büyük PDF'ler için Celery task'ına taşınacak.
     Upload endpoint UPLOADED dönecek, işlem arka planda.

  3. RAG search endpoint:
     Kullanıcı sorusu → embedding → benzer chunk'lar →
     LLM'e context → kaynaklı cevap.

  4. models/__init__.py düzeltmesi:
     DocumentChunk __all__'da var ama import satırı eksik.
     from app.models.document_chunk import DocumentChunk eklenecek.

========================================================
========================================================
COMMIT: "Add embedding, semantic search and RAG chat pipeline"
— EMBEDDING, SEMANTİK ARAMA VE RAG CHAT KATMANININ EKLENMESİ
========================================================

Bu commit ile RAG pipeline'ının son ve en kritik aşaması tamamlandı.
Artık sistem dokümanları anlıyor, sorular sorabiliyor ve kaynaklı
cevap üretebiliyor.

Etkilenen dosyalar (migration hariç):
  GÜNCELLEME : core/config.py
  GÜNCELLEME : models/document_chunk.py   (embedding kolonu eklendi)
  GÜNCELLEME : services/document_processing_service.py
  GÜNCELLEME : api/v1/router.py
  YENİ       : services/embedding_service.py
  YENİ       : services/semantic_search_service.py
  YENİ       : services/llm_service.py
  YENİ       : services/rag_service.py
  YENİ       : schemas/search.py
  YENİ       : schemas/chat.py
  YENİ       : api/v1/search.py
  YENİ       : api/v1/chat.py
  YENİ       : alembic/versions/7914a55ba2b2_add_embedding_column...

========================================================
CORE/CONFIG.PY — GÜNCELLEME: GEMİNİ AYARLARI EKLENDİ
========================================================

Yeni alanlar:

  EMBEDDING_PROVIDER: str = "gemini"
    → Gelecekte "openai" gibi başka provider'a geçiş için.
      Şu an sadece "gemini" destekleniyor.

  GEMINI_API_KEY: str | None = None
    → Google Gemini API anahtarı. .env'den okunur.
    → None olabilir: embedding servisi başlarken kontrol eder,
      eksikse ValueError fırlatır, uygulama anlamlı hata verir.

  EMBEDDING_MODEL: str = "gemini-embedding-001"
    → Embedding üretmek için kullanılan Gemini modeli.

  EMBEDDING_DIMENSION: int = 1536
    → Üretilen vektörün boyutu.
    → pgvector'da Vector(1536) ile BİREBİR uyumlu olmalı.
    → Bu iki değer farklı olursa vektörler DB'ye yazılamaz.

  CHAT_MODEL: str = "gemini-2.0-flash"
    → Soru-cevap için kullanılan LLM modeli.
    → config'den okunduğu için model değişimi tek satır.

  NOT: .env dosyasında CHAT_MODEL=gemini-3.1-flash-lite yazıyor.
    Bu model adı geçersiz. Doğrusu "gemini-2.0-flash".
    .env düzeltilmezse runtime'da Gemini API hata verir.
    config.py default'u doğru, .env güncellenmeli.

========================================================
SERVICES/EMBEDDING_SERVICE.PY — YENİ: EMBEDDING SERVİSİ
========================================================

## GENEL AMAÇ
Metin → vektör (list[float]) dönüşümünü yönetir.
Google Gemini API kullanarak hem tekil hem toplu embedding üretir.

## get_gemini_client() → genai.Client
  Gemini API istemcisini oluşturur.
  Anahtarsız çağrılırsa ValueError fırlatır — erken hata tespiti.
  Her çağrıda yeni client oluşturulur (bağlantı havuzu değil).

## create_embedding(text) → list[float]
  Tek bir metin için 1536 boyutlu vektör döner.
  Kullanım yeri: semantic_search_service.py — kullanıcı sorgusunu
  vektöre çevirirken.

  client.models.embed_content(
      model=settings.EMBEDDING_MODEL,
      contents=text,
      config=types.EmbedContentConfig(
          output_dimensionality=settings.EMBEDDING_DIMENSION,
      ),
  )

  output_dimensionality neden önemli?
    → Gemini modeli farklı boyutlarda vektör üretebilir.
      Biz 1536 istiyoruz çünkü DB'deki Vector(1536) ile
      uyumsuzluk olursa vektör yazılamaz/okunamaz.
    → Config'den okunuyor: boyut değiştirilmek istenirse
      tek yerden ayarlanır.

## create_embeddings(texts) → list[list[float]]
  Birden fazla metni tek API çağrısında toplu işler.
  Kullanım yeri: document_processing_service.py — tüm chunk'ları
  tek seferde vektörleştirirken.

  Neden toplu?
    → 500 chunk olan bir doküman için 500 ayrı API çağrısı yerine
      tek çağrı. Hem maliyet hem süre dramatik şekilde azalır.
    → texts boşsa API çağrısı yapılmaz, boş liste döner.
      (Gereksiz ağ trafiği önlenir.)

========================================================
MODELS/DOCUMENT_CHUNK.PY — GÜNCELLEME: EMBEDDING KOLONU
========================================================

Modele eklenen tek alan:

  embedding: Mapped[list[float] | None] = mapped_column(
      Vector(1536),
      nullable=True,
  )

  pgvector.sqlalchemy.Vector tipi kullanıldı.
    → pgvector PostgreSQL eklentisinin SQLAlchemy bağlayıcısı.
    → Vector(1536): 1536 float'tan oluşan sabit boyutlu vektör.

  nullable=True neden?
    → Migration sırasında mevcut chunk'lar embedding olmadan
      kalır, sistem çökmez.
    → semantic_search_service.py embedding.isnot(None) filtresi
      ile embedding'siz chunk'ları aramadan dışlar.

========================================================
MİGRASYON: 7914a55ba2b2 — EMBEDDING KOLONU EKLENDİ
========================================================

  Revision    : 7914a55ba2b2
  Revises     : af7df02fecaa (document_chunks tablosu migration'ı)
  Create Date : 2026-05-18

  upgrade():
    op.execute("CREATE EXTENSION IF NOT EXISTS vector")
      → pgvector eklentisini aktif eder.
      → IF NOT EXISTS: zaten yüklüyse hata vermez.
      → Bu komut olmadan Vector tipi PostgreSQL'de çalışmaz.

    op.add_column("document_chunks", Column("embedding", Vector(1536)))
      → Mevcut tabloya yeni kolon eklenir.
      → nullable=True: mevcut satırlarda NULL olarak kalır.

  downgrade():
    op.drop_column("document_chunks", "embedding")
      → Kolonu kaldırır. Eklenti kaldırılmaz (başka tablolar
        da kullanıyor olabilir).

  Komutlar:
    alembic revision --autogenerate -m "add embedding column..."
    alembic upgrade head

========================================================
SERVICES/DOCUMENT_PROCESSING_SERVICE.PY — GÜNCELLEME
========================================================

## DEĞİŞEN KISIM: embedding üretimi eklendi

Önceki hal:
  parse → chunk → DB'ye yaz

Yeni hal:
  parse → chunk → embedding üret → DB'ye yaz

Eklenen satırlar:

  chunk_texts = [chunk["content"] for chunk in chunks]
  embeddings = create_embeddings(chunk_texts)

  Neden önceden toplanıp tek çağrı?
    → Her chunk için ayrı create_embedding() çağırmak yerine
      tüm chunk içerikleri liste halinde toplandı, tek API çağrısı.
    → 100 chunk = 100 API çağrısı (eski) → 1 API çağrısı (yeni).

  DocumentChunk oluşturulurken:
    embedding=embeddings[index]   ← YENİ
    → index ile chunk ve embeddings listesi senkronize.
    → create_embeddings() sırayı korur: embeddings[0] chunks[0]'a ait.

  Hata yönetimi değişmedi:
    → API hatası (geçersiz key, kota aşımı) exception fırlatır.
    → except Exception bloğu yakalar, rollback + status=FAILED.

========================================================
SERVICES/SEMANTIC_SEARCH_SERVICE.PY — YENİ: SEMANTİK ARAMA
========================================================

## GENEL AMAÇ
Kullanıcının metin sorgusunu vektöre çevirir, pgvector'da en
benzer chunk'ları bulur, formatlanmış sonuçları döner.

## semantic_search_chunks(db, organization_id, query, top_k=5)

ADIM 1 — Sorguyu vektörleştir:
  query_embedding = create_embedding(query)
    → Kullanıcı ne yazarsa yazsın, aynı embedding modeli kullanılır.
    → Dokümanlar da aynı modelle vektörleştirildiği için
      "elma" sorgusu "meyve" içerikli chunk'ı bulabilir (semantik).

ADIM 2 — Cosine distance hesapla:
  distance = DocumentChunk.embedding.cosine_distance(query_embedding)
    → pgvector'ın SQLAlchemy operatörü. SQL'de <=> operatörüne karşılık.
    → Cosine distance: 0 = özdeş, 2 = tam zıt.
    → Neden cosine? Metin embedding'lerinde vektör büyüklüğü değil
      yönü önemli. Cosine açı ölçer, büyüklükten bağımsız çalışır.
      Euclidean distance metin benzerliğinde daha kötü sonuç verir.

ADIM 3 — Filtreli sorgu:
  db.query(DocumentChunk, Document.original_file_name, distance)
    .join(Document, Document.id == DocumentChunk.document_id)
    .filter(
        DocumentChunk.organization_id == organization_id,  ← tenant isolation
        DocumentChunk.embedding.isnot(None),               ← embedding'siz atla
    )
    .order_by(distance)   ← en benzer önce
    .limit(top_k)

  Tenant isolation neden kritik?
    → organization_id filtresi olmasaydı kullanıcı başka
      şirketin dokümanlarında arama yapabilirdi.
    → Bu filtre her aramada çalışır, bypass edilemez.

  .isnot(None) neden?
    → embedding=NULL olan chunk'larla cosine_distance hesaplanamaz,
      DB hatası fırlatır. Filtre ile bu chunk'lar tamamen dışlanır.

ADIM 4 — Similarity score dönüşümü:
  similarity_score = 1 - float(distance_value)
    → Cosine distance (0-2) → 1'den çıkarınca (1 ile -1 arası)
    → Tam benzer chunk yaklaşık 1.0, alakasız chunk 0'a yakın değer
    → UI'da gösterilmesi için sezgisel: yüksek = benzer

ADIM 5 — Dict formatında dön:
  ORM nesnesi değil dict döner.
    → response şemasıyla birebir uyumlu.
    → JSON serialize sırasında ORM detachment hatası olmaz.
    → search.py ve rag_service.py her ikisi de bu dict'i kullanır.

========================================================
SCHEMAS/SEARCH.PY — YENİ: ARAMA ŞEMALARI
========================================================

## SearchRequest
  query: str = Field(min_length=2, max_length=1000)
    → Çok kısa arama önlenir. 1000 karakter üstü gereksiz API yükü.
  top_k: int = Field(default=5, ge=1, le=20)
    → Kaç sonuç dönsün. 1-20 arası kısıt: 0 veya 1000 istenemez.

## SearchResult
  document_id, original_file_name, chunk_id, chunk_index
  content, page_number, sheet_name, source_type, similarity_score

  Neden content var?
    → Arama ekranında eşleşen chunk içeriği önizleme olarak gösterilir.
    → Chat endpoint'inde content RAG context'e dahil edilir.

========================================================
API/V1/SEARCH.PY — YENİ: SEMANTİK ARAMA ENDPOINT'İ
========================================================

POST /api/v1/search
  request : SearchRequest (body)
  response: list[SearchResult]

  Akış:
    1. get_current_user()                        → kimlik doğrulama
    2. get_current_user_primary_organization_id() → hangi org?
    3. semantic_search_chunks(db, org_id, query, top_k)
    4. list[SearchResult] → response

  Neden ayrı endpoint (chat'ten bağımsız)?
    → Kullanıcı "hangi dokümanlar bu konudan bahsediyor?" diye
      cevap üretmeden sadece chunk aramak isteyebilir.
    → Test ve debug için: embedding kalitesini chat olmadan ölçmek.
    → Frontend'de "Bu konu hangi dosyalarda geçiyor?" widget'ı.

========================================================
SERVICES/LLM_SERVICE.PY — YENİ: LLM (DİL MODELİ) SERVİSİ
========================================================

## GENEL AMAÇ
Arama sonuçlarından oluşturulan context metni ve kullanıcı sorusunu
Gemini'ye gönderir, Türkçe cevap üretir.

## generate_answer_with_context(question, context) → str

  Prompt tasarımı:
    "Sen kurumsal dokümanlar üzerinde çalışan bir RAG asistanısın."
    → Sistem rolü açıkça tanımlandı.

    "Sadece verilen kaynak metinlere dayanarak cevap ver."
    → Hallüsinasyon engeli: LLM kendi bilgisinden uydurma yapamaz.

    "Kaynaklarda bilgi yoksa 'Bu bilgi verilen dokümanlarda
     bulunamadı.' de."
    → Sabit geri dönüş metni → UI bu cevabı tespit edip
      farklı gösterebilir.

    "Uydurma bilgi ekleme."
    → RAG prensibi: LLM sadece verilen context'i yorumlar.

  Neden Türkçe prompt?
    → Hedef kullanıcılar Türkçe doküman yükleyecek.
    → Gemini Türkçe promptu anlıyor, Türkçe cevap üretiyor.
    → İleride dil config'den yönetilebilir.

  response.text or "Cevap üretilemedi."
    → Gemini boş response döndürdüğünde güvenli fallback.

========================================================
SERVICES/RAG_SERVICE.PY — YENİ: RAG ORKESTRATÖRü
========================================================

## GENEL AMAÇ
Semantic search + context oluşturma + LLM çağrısını koordine eder.
Chat endpoint'i sadece bu servisi çağırır, detayları bilmez.

## build_context_from_search_results(search_results) → str

  Her chunk için:
    [Kaynak 1]
    Dosya: rapor.pdf | Tür: pdf | Sayfa: 5
    İçerik:
    ...metin...

    ---

    [Kaynak 2]
    ...

  Neden bu format?
    → LLM her kaynağın hangi dosyadan geldiğini görerek
      "Kaynak 1'de belirtildiği üzere..." diyebilir.
    → Sayfa/sheet bilgisi → LLM cevabında kaynak atıfı yapabilir.
    → "---" ayırıcı → kaynaklar arası sınır net.

## answer_question_with_rag(db, organization_id, question, top_k=5)

  ADIM 1: semantic_search_chunks() → chunk'ları bul
  ADIM 2: search_results boşsa → LLM'e gitmeden erken dön
    → "Bu bilgi verilen dokümanlarda bulunamadı."
    → Neden? Boş context'le LLM'e gidilirse hallüsinasyon riski.
      Gereksiz API çağrısı ve maliyet de önlenir.
  ADIM 3: build_context_from_search_results() → context metni
  ADIM 4: generate_answer_with_context() → LLM cevabı
  ADIM 5: sources listesi hazırla (chunk metadata'sı)
  ADIM 6: {"answer": ..., "sources": [...]} döndür

  sources neden ayrı döndürülüyor?
    → Frontend "Bu cevap şu dosyanın şu sayfasından geldi"
      bilgisini gösterir → kullanıcı doğrulamak isterse
      orjinal dokümana bakabilir.
    → similarity_score da sources içinde → UI güven skoru gösterebilir.

========================================================
SCHEMAS/CHAT.PY — YENİ: CHAT ŞEMALARI
========================================================

## ChatRequest
  question: str = Field(min_length=2, max_length=2000)
    → Aramadan farklı olarak 2000 karakter: soru daha uzun olabilir.
  top_k: int = Field(default=5, ge=1, le=20)
    → Kaç kaynak chunk LLM'e verilsin.
    → Fazla chunk: daha fazla bilgi ama daha uzun/pahalı prompt.
    → Az chunk: hızlı ama bilgi eksik kalabilir. 5 makul denge.

## ChatSource
  document_id, original_file_name, chunk_id, chunk_index
  page_number, sheet_name, source_type, similarity_score
    → UI'da her cevabın altında "kaynaklar" bölümü için.

## ChatResponse
  answer: str
    → LLM'in ürettiği Türkçe cevap.
  sources: list[ChatSource]
    → Cevabı destekleyen chunk'ların listesi.

========================================================
API/V1/CHAT.PY — YENİ: CHAT ENDPOINT'İ
========================================================

POST /api/v1/chat
  request : ChatRequest (body)
  response: ChatResponse

  Akış:
    1. get_current_user()                        → kimlik doğrulama
    2. get_current_user_primary_organization_id() → hangi org?
    3. answer_question_with_rag(db, org_id, question, top_k)
    4. ChatResponse → response

  answer_question_with_rag() dict döndürür, ChatResponse bunu
  otomatik serialize eder (FastAPI response_model özelliği).

  Neden iş mantığı endpoint'te değil service'te?
    → Chat endpoint sadece HTTP koordinasyonu yapar.
    → RAG mantığı (search → context → LLM) rag_service.py'da.
    → Test: rag_service unit test edilebilir, HTTP katmanı mock'lanır.
    → Celery: ileride chat işlemi arka plana alınmak istenirse
      endpoint değil sadece service taşınır.

========================================================
API/V1/ROUTER.PY — GÜNCELLEME
========================================================

Önceki hal:
  from app.api.v1 import auth, users, organizations, documents
  (search ve chat yoktu)

Yeni hal:
  from app.api.v1 import auth, users, organizations, documents, search, chat
  api_router.include_router(search.router)
  api_router.include_router(chat.router)

GÜNCEL URL TABLOSU (tüm sistem):
  POST /api/v1/auth/register              → kayıt
  POST /api/v1/auth/login                 → giriş
  GET  /api/v1/users/me                   → profil (token gerekli)
  GET  /api/v1/organizations/me           → organizasyonlar (token gerekli)
  POST /api/v1/documents/upload           → dosya yükle (token gerekli)
  GET  /api/v1/documents                  → dosya listesi (token gerekli)
  GET  /api/v1/documents/{id}             → dosya detayı (token gerekli)
  GET  /api/v1/documents/{id}/chunks      → chunk listesi (token gerekli)
  POST /api/v1/search                     → semantik arama (token gerekli)
  POST /api/v1/chat                       → RAG soru-cevap (token gerekli)

========================================================
TAM RAG PIPELINE AKIŞI — BÜYÜK RESİM (SİSTEM TAMAMLANDI)
========================================================

DOKÜMAN YÜKLEME:
  POST /documents/upload + Bearer <token>
        ↓
  parse_document()           → sections (PDF: sayfa, Excel: sheet)
        ↓
  create_chunks_from_sections() → chunk dict listesi (overlap=200)
        ↓
  create_embeddings(chunk_texts) → Gemini API → list[list[float]]
        ↓
  DocumentChunk(..., embedding=...) → DB'ye kayıt (status=COMPLETED)

ARAMA:
  POST /search {"query": "fesih şartları", "top_k": 5}
        ↓
  create_embedding(query)    → Gemini API → query vektörü
        ↓
  cosine_distance sorgusu    → pgvector <=> operatörü
        ↓
  organization_id filtreli   → sadece kendi org'un chunk'ları
        ↓
  list[SearchResult]         → similarity_score ile sıralı

SORU-CEVAP:
  POST /chat {"question": "Bu sözleşmede fesih şartları nelerdir?"}
        ↓
  semantic_search_chunks()   → top_k benzer chunk
        ↓
  build_context_from_search_results()
        ↓
  [Kaynak 1] Dosya: contract.pdf | Sayfa: 12 | İçerik: ...
  [Kaynak 2] ...
        ↓
  generate_answer_with_context() → Gemini gemini-2.0-flash
        ↓
  {
    "answer": "Madde 8.2'ye göre taraflar 30 gün önceden...",
    "sources": [{"original_file_name": "contract.pdf",
                  "page_number": 12,
                  "similarity_score": 0.94, ...}]
  }

GÜVENLİK ÖZETİ (bu commit):
  - Tenant isolation: her embedding araması organization_id filtreli
  - Hallüsinasyon engeli: LLM sadece verilen context'e cevap verir
  - Boş context kontrolü: chunk bulunamazsa LLM'e hiç gidilmez
  - API key doğrulama: eksikse uygulama başlamadan ValueError

TEKNIK BORÇ / SONRAKI ADIMLAR:
  - .env'deki CHAT_MODEL=gemini-3.1-flash-lite geçersiz model adı,
    "gemini-2.0-flash" olarak düzeltilmeli.
  - Celery entegrasyonu: embedding üretimi (büyük dosyalar için)
    arka plana alınmalı, şu an senkron çalışıyor.
  - Chunk overlap ve boyutu (1200/200) RAG kalitesine göre
    tune edilebilir, config'e taşınabilir.
  - LLM prompt dili config'e alınabilir (Türkçe/İngilizce).
  - Token kullanım takibi: Gemini API harcamasını DB'ye yazma.

========================================================

========================================================
COMMIT: "Add chat history tables"
— SOHBET GEÇMİŞİ KATMANININ EKLENMESİ
========================================================

Bu commit ile sistemin sohbet geçmişi altyapısı kuruldu.
Artık kullanıcıların sorduğu sorular ve sistemin ürettiği cevaplar
oturum (session) bazında veritabanında saklanıyor. Her cevabın
hangi kaynak chunk'lardan üretildiği de kayıt altına alınıyor.

Etkilenen dosyalar (migration hariç):
  GÜNCELLEME : models/__init__.py
  GÜNCELLEME : schemas/chat.py    (session_id alanı + yeni şemalar)
  GÜNCELLEME : api/v1/chat.py     (session yönetimi eklendi)
  YENİ       : models/chat.py
  YENİ       : services/chat_history_service.py
  YENİ       : alembic/versions/2d176fb1b7b2_add_chat_history_tables.py

========================================================
MODELS/CHAT.PY — YENİ: CHAT GEÇMİŞİ MODELLERİ
========================================================

## ChatMessageRole(str, Enum)

class ChatMessageRole(str, Enum):
    USER      = "user"
    ASSISTANT = "assistant"

  str, Enum kombinasyonu neden?
    → str  : DB'ye "user"/"assistant" string olarak yazılır,
             JSON serialize olur, karşılaştırma doğal çalışır.
    → Enum : "useer" gibi typo ile geçersiz rol atanması önlenir.
    → Sadece iki rol var: kullanıcı mesajı veya asistan cevabı.
      OpenAI/Gemini API sözleşmesiyle uyumlu isimler seçildi.

## ChatSession(Base) — "chat_sessions" tablosu

  Bir sohbet oturumunu temsil eder. Kullanıcı her yeni konuya
  başladığında yeni bir session açılır, sonraki mesajlar aynı
  session'a bağlanır.

  organization_id: ForeignKey("organizations.id", ondelete="CASCADE")
    → Her session bir organizasyona aittir.
    → ondelete="CASCADE": Organizasyon silinince o organizasyonun
      tüm sohbet geçmişi de silinir. Orphan kayıt kalmaz.
    → index=True: "Bu organizasyonun tüm oturumları" sorgusu için.

  user_id: ForeignKey("users.id", ondelete="SET NULL"), nullable=True
    → Session'ı başlatan kullanıcıyı tutar.
    → ondelete="SET NULL" (CASCADE değil): Kullanıcı silinse bile
      sohbet geçmişi korunur, user_id NULL olur.
    → nullable=True: Sistem tarafından başlatılan sohbetlere
      esneklik bırakıldı (şu an kullanılmıyor ama mimari açık).

  title: String(255), nullable=True
    → Oturumun başlığı. Şu an ilk sorunun ilk 80 karakteri kullanılır.
    → NULL bırakılabilir: başlık sonradan da set edilebilir.
    → Kullanıcıya sohbet listesinde gösterilecek.

  created_at / updated_at: DateTime, default=datetime.utcnow
    → updated_at onupdate: Yeni mesaj eklendikçe güncellenir.
    → list_chat_sessions() ORDER BY updated_at.desc() ile
      en son aktif oturum önce gelir.

  messages = relationship("ChatMessage", cascade="all, delete-orphan")
    → session.messages ile tüm mesajlara ORM üzerinden erişilir.
    → cascade="all, delete-orphan": Session silinince mesajlar da silinir.
    → back_populates="session": iki yönlü ilişki (ChatMessage.session).

## ChatMessage(Base) — "chat_messages" tablosu

  Bir oturum içindeki tek bir mesajı temsil eder.
  Hem kullanıcı soruları hem asistan cevapları burada saklanır.

  session_id: ForeignKey("chat_sessions.id", ondelete="CASCADE")
    → Hangi oturuma ait olduğu.
    → ondelete="CASCADE": Session silinince mesajlar da silinir.
    → index=True: "Bu session'ın mesajları" sorgusu için.

  role: String(50), nullable=False, index=True
    → "user" veya "assistant" string değeri.
    → ChatMessageRole.value ile set edilir.
    → index=True: Role göre filtreleme yapılabilir
      (ör: "sadece asistan mesajlarını getir").

  content: Text, nullable=False
    → Mesajın gerçek içeriği. Text tipi: uzun cevaplar için sınırsız.
    → String(N) kullanılmadı: LLM cevabı 4000 karakter olabilir.

  sources = relationship("ChatSource", cascade="all, delete-orphan")
    → Bir asistan mesajına bağlı kaynak chunk'lar.
    → Kullanıcı mesajlarının sources listesi boş olur (sadece
      asistan mesajları kaynak barındırır).

## ChatSource(Base) — "chat_sources" tablosu

  Bir asistan mesajını destekleyen kaynak chunk'ı temsil eder.
  RAG'ın "kaynaklı cevap" özelliğinin veritabanı yansıması.

  message_id: ForeignKey("chat_messages.id", ondelete="CASCADE")
    → Hangi asistan mesajına ait.
    → ondelete="CASCADE": Mesaj silinince kaynaklar da silinir.

  document_id: ForeignKey("documents.id", ondelete="CASCADE")
    → Kaynak doküman. Doküman silinirse bu kayıt da silinir.

  chunk_id: ForeignKey("document_chunks.id", ondelete="CASCADE")
    → Hangi chunk kullanıldı. Chunk silinirse bu kayıt da silinir.

  original_file_name: String(255), nullable=False
    → Gösterim için dosya adı. Document silinse bile adı kalır mı?
      Hayır, ondelete="CASCADE" ile bu kayıt da silinir.
      Var olduğu sürece orijinal ad tutulur.

  chunk_index: Integer, nullable=False
    → Chunk'ın belgedeki sırası. Debug ve kaynak atıfı için.

  page_number: Integer, nullable=True
    → PDF için sayfa numarası. Diğer dosya türlerinde NULL.

  sheet_name: String(255), nullable=True
    → Excel için sheet adı. Diğer dosya türlerinde NULL.

  source_type: String(50), nullable=True
    → "pdf", "docx", "xlsx", "txt". UI'da ikon/etiket için.

  similarity_score: Float, nullable=False
    → Bu chunk'ın soruyla olan benzerlik skoru (0-1 arası).
    → UI'da güven çubuğu veya "yüzde kaç alakalı" gösterimi için.

========================================================
MODELS/__INIT__.PY — GÜNCELLEME
========================================================

Önceki __all__:
  ["User", "Organization", "UserOrganizationRole",
   "Document", "DocumentChunk"]

Yeni __all__:
  ["User", "Organization", "UserOrganizationRole",
   "Document", "DocumentChunk",
   "ChatSession", "ChatMessage", "ChatSource"]

Eklenen import satırı:
  from app.models.chat import ChatSession, ChatMessage, ChatSource

  Neden zorunlu?
    → Alembic Base.metadata'yı tarar. chat.py hiçbir yerden
      import edilmeseydi 3 yeni tablo migration'da görünmezdi.
    → Bu satır Alembic'in üç tabloyu fark etmesini sağladı.

========================================================
SCHEMAS/CHAT.PY — GÜNCELLEME: SESSION DESTEĞİ EKLENDİ
========================================================

## ChatRequest — DEĞİŞİKLİK

Yeni alan:
  session_id: int | None = None

  Neden eklendi?
    → Mevcut bir oturuma devam etmek için client session_id gönderir.
    → None gönderilirse yeni oturum otomatik oluşturulur.
    → Opsiyonel (None default): API geriye dönük uyumlu kaldı.
      Eski istemciler session_id göndermeden çalışmaya devam eder.

## ChatMessageRead — YENİ ŞEMA
  id, session_id, role, content, created_at
  model_config = {"from_attributes": True}

  GET /chat/sessions/{session_id} endpoint'inin mesaj listesi için.
  Kullanıcı geçmiş sohbeti görüntüleyebilir.

## ChatSessionRead — YENİ ŞEMA
  id, organization_id, user_id, title, created_at, updated_at
  model_config = {"from_attributes": True}

  GET /chat/sessions listesinin her elemanı için.
  Kullanıcı oturum listesini (başlık + tarih) görebilir.

## ChatSessionDetail — YENİ ŞEMA
  ChatSessionRead'den kalıtım alır.
  messages: list[ChatMessageRead] alanı eklendi.

  GET /chat/sessions/{session_id} endpoint'inin tam yanıt şeması.
  Oturum bilgisi + altındaki tüm mesajlar tek request'te gelir.

  Neden iki ayrı şema (ChatSessionRead vs ChatSessionDetail)?
    → Liste endpoint'inde mesaj içeriklerini döndürmek gerekmez.
      Binlerce mesajlı oturum listesi gereksiz veri taşır.
    → Detail endpoint'inde tam geçmiş isteniyor.
    → İki ayrı şema ile her endpoint tam ihtiyacı kadar veri döner.

========================================================
SERVICES/CHAT_HISTORY_SERVICE.PY — YENİ DOSYA
========================================================

## GENEL AMAÇ
Chat geçmişine ait tüm veritabanı işlemlerini tek bir service
altında toplar. API katmanı doğrudan ORM nesnesi yaratmaz,
bu servisi çağırır.

## create_chat_session(db, organization_id, user_id, title) → ChatSession

  ChatSession ORM nesnesi oluşturur, db.add eder, db.flush() çağırır.

  db.flush() neden (commit değil)?
    → Çağıran (api/v1/chat.py) tüm işlemleri tek bir db.commit()
      ile sonlandırır. flush() ile session.id alınır (sequence
      ID üretildi) ama transaction henüz kapatılmaz.
    → Mesaj veya kaynak kaydı başarısız olursa session da geri alınır.
    → Atomicity: session + mesaj + kaynaklar ya hepsi ya hiçbiri.

## get_chat_session_by_id(db, session_id, organization_id, user_id)
   → ChatSession | None

  Üç filtre birlikte kullanılır:
    ChatSession.id == session_id
    ChatSession.organization_id == organization_id
    ChatSession.user_id == user_id

  Neden üç filtre?
    → Sadece id filtresi kullansaydık başka kullanıcının
      session_id'sini bilerek ona mesaj eklenebilirdi.
    → organization_id: başka organizasyonun oturumuna erişim engeli.
    → user_id: aynı organizasyonun başka kullanıcısının
      oturumuna erişim engeli.
    → Tüm filtrelerin aynı anda geçmesi zorunlu.

## list_chat_sessions(db, organization_id, user_id)
   → list[ChatSession]

  İki filtre: organization_id ve user_id.
  ORDER BY updated_at.desc(): En son mesaj alınan oturum önce.
  UI'da WhatsApp/mesaj uygulaması gibi sıralama.

## create_chat_message(db, session_id, role, content) → ChatMessage

  role=ChatMessageRole.USER veya ChatMessageRole.ASSISTANT alır.
  DB'ye role.value (string) yazılır, Enum değeri değil.
  db.flush(): session kaydında olduğu gibi transaction açık kalır.

## create_chat_sources(db, message_id, sources) → list[ChatSource]

  sources: list[dict] alır (rag_service.py'den dönen format).
  Her dict için bir ChatSource ORM nesnesi oluşturur.
  db.flush(): tüm kaynaklar eklendikten sonra toplu flush.
  Neden liste döner?
    → Şu an dönüş değeri kullanılmıyor ama ileride
      "hangi kaynaklar eklendi" bilgisi lazım olabilir.

========================================================
API/V1/CHAT.PY — GÜNCELLEME: SESSION YÖNETİMİ EKLENDİ
========================================================

## POST /api/v1/chat — BÜYÜK DEĞİŞİKLİK

Önceki hal:
  RAG sonucu hesaplanır, direkt ChatResponse döner.
  Hiçbir şey veritabanına kaydedilmez.

Yeni hal:
  1. session_id varsa → mevcut session doğrula (404 kontrolü)
  2. session_id yoksa → yeni session oluştur (başlık = sorunun ilk 80 karakteri)
  3. Kullanıcı mesajını kaydet (role=USER)
  4. RAG pipeline çalıştır → cevap + kaynaklar
  5. Asistan mesajını kaydet (role=ASSISTANT)
  6. Kaynak chunk'ları kaydet (ChatSource)
  7. db.commit() — her şey tek transaction
  8. ChatResponse döner

  Neden başlık sorunun ilk 80 karakteri?
    → Kullanıcıya anlamlı başlık göstermek için.
    → Otomatik üretim: kullanıcıdan başlık girmesi istenmez.
    → 80 karakter: liste görünümünde kesilmeden gösterilebilir.
    → İleride LLM ile özet başlık üretilebilir.

  Neden session_id yoksa 404 fırlatılıyor?
    → client başka bir kullanıcıya ait session_id gönderse,
      organizasyon veya user_id eşleşmezse None döner → 404.
    → Güvenlik: yanlış session'a mesaj eklenemez.

  db.commit() neden en sonda, tek seferde?
    → Kullanıcı mesajı, RAG, asistan mesajı, kaynaklar: hepsi
      ya birlikte yazılır ya da hiç yazılmaz.
    → RAG pipeline hata verirse hiçbir mesaj DB'ye gitmez,
      tutarsız (sorusu olan ama cevabı olmayan) kayıt oluşmaz.

## GET /api/v1/chat/sessions — YENİ ENDPOINT

  response_model=list[ChatSessionRead]
  list_chat_sessions(db, org_id, user_id) çağırır.
  Giriş yapmış kullanıcının tüm oturumlarını döner.
  Başlık + tarih bilgisi → sohbet listesi ekranı için.

## GET /api/v1/chat/sessions/{session_id} — YENİ ENDPOINT

  response_model=ChatSessionDetail
  get_chat_session_by_id() → 404 kontrolü → return session

  ChatSessionDetail içinde messages alanı var.
  SQLAlchemy lazy load ile session.messages otomatik yüklenir.
  Tüm sohbet geçmişi tek istekte gelir.

  Neden messages ayrı endpoint'te değil, bu yanıtta?
    → Sohbet ekranı açıldığında hem oturum bilgisi hem de tüm
      mesajlar aynı anda lazım. İki ayrı istek yerine tek istek
      daha iyi kullanıcı deneyimi ve daha az ağ gecikmesi.

========================================================
ALEMBİC MİGRASYONU: 2d176fb1b7b2_add_chat_history_tables.py
========================================================

  Revision    : 2d176fb1b7b2
  Revises     : 7914a55ba2b2 (embedding kolonu migration'ı)
  Create Date : 2026-05-18 15:59:28

  upgrade() ile oluşturulan tablolar ve yapılar:

  1. chat_sessions tablosu
       id, organization_id, user_id, title, created_at, updated_at
       FK: organization_id → organizations.id (ondelete=CASCADE)
       FK: user_id → users.id (ondelete=SET NULL)
       Index'ler:
         ix_chat_sessions_id
         ix_chat_sessions_organization_id
         ix_chat_sessions_user_id

  2. chat_messages tablosu
       id, session_id, role, content, created_at
       FK: session_id → chat_sessions.id (ondelete=CASCADE)
       Index'ler:
         ix_chat_messages_id
         ix_chat_messages_session_id
         ix_chat_messages_role

  3. chat_sources tablosu
       id, message_id, document_id, chunk_id
       original_file_name, chunk_index, page_number,
       sheet_name, source_type, similarity_score, created_at
       FK: message_id → chat_messages.id (ondelete=CASCADE)
       FK: document_id → documents.id (ondelete=CASCADE)
       FK: chunk_id → document_chunks.id (ondelete=CASCADE)
       Index'ler:
         ix_chat_sources_id
         ix_chat_sources_message_id
         ix_chat_sources_document_id
         ix_chat_sources_chunk_id

  downgrade(): Ters sırayla tablo ve index'ler drop edilir.
    chat_sources → chat_messages → chat_sessions
    (FK bağımlılığı nedeniyle alt tablo önce silinmeli)

  Çalıştırılan komutlar (Windows terminalinden):

    alembic revision --autogenerate -m "add chat history tables"
      → Mevcut modeller ile DB şeması karşılaştırıldı.
      → 3 yeni tablo ve 11 yeni index tespit edildi.
      → 2d176fb1b7b2_add_chat_history_tables.py dosyası oluşturuldu.

    alembic upgrade head
      → Önceki migration (7914a55ba2b2) → yeni migration (2d176fb1b7b2)
        geçişi uygulandı.
      → PostgreSQL'de 3 tablo ve 11 index fiziksel olarak oluşturuldu.

  Terminalde görülen collation uyarısı hakkında:
    WARNING: database "rag_db" has a collation version mismatch
    → Veri bozulması veya hata DEĞİL, sadece uyarı.
    → DB PostgreSQL 2.41 collation ile oluşturulmuş,
      mevcut sistem kütüphanesi 2.36 sürümünü sunuyor.
    → Migration çalışmasını engellemedi, tüm tablolar başarıyla oluştu.
    → Düzeltmek için: ALTER DATABASE rag_db REFRESH COLLATION VERSION;
      (Zorunlu değil, production'da dikkat edilmeli.)

========================================================
TABLO İLİŞKİ ZİNCİRİ — CASCADE SİLME AKIŞI
========================================================

  organizations silinirse:
    → chat_sessions (CASCADE)
        → chat_messages (CASCADE)
            → chat_sources (CASCADE)
    → documents (CASCADE)
        → document_chunks (CASCADE) → chat_sources (CASCADE)

  users silinirse:
    → chat_sessions.user_id = NULL (SET NULL, session korunur)
    → documents.uploaded_by_user_id = NULL (SET NULL)

  documents silinirse:
    → document_chunks (CASCADE) → chat_sources (CASCADE)
    → chat_sources.document_id (CASCADE, doğrudan)

  document_chunks silinirse:
    → chat_sources.chunk_id (CASCADE)

  Tasarım kararı:
    → Kullanıcı silinse bile organizasyonun sohbet geçmişi korunur.
    → Doküman silinse bile mesajın kendisi (ChatMessage.content) korunur,
      sadece o mesajın kaynakları (ChatSource) temizlenir.

========================================================
YENİ URL TABLOSU (GÜNCELLENMİŞ TAM LİSTE)
========================================================

  POST /api/v1/auth/register              → kayıt
  POST /api/v1/auth/login                 → giriş
  GET  /api/v1/users/me                   → profil
  GET  /api/v1/organizations/me           → organizasyonlar
  POST /api/v1/documents/upload           → dosya yükle
  GET  /api/v1/documents                  → dosya listesi
  GET  /api/v1/documents/{id}             → dosya detayı
  GET  /api/v1/documents/{id}/chunks      → chunk listesi
  POST /api/v1/search                     → semantik arama
  POST /api/v1/chat                       → RAG soru-cevap (+ session)
  GET  /api/v1/chat/sessions              → sohbet oturumu listesi
  GET  /api/v1/chat/sessions/{id}         → oturum detayı + mesajlar

========================================================
CHAT ENDPOINT'İNİN GÜNCEL TAM AKIŞI
========================================================

POST /api/v1/chat {"question": "...", "top_k": 5, "session_id": null}
        ↓
get_current_user() → token doğrulama
        ↓
get_current_user_primary_organization_id() → org_id
        ↓
session_id var mı?
  EVET → get_chat_session_by_id() → bulunamadı? → 404
  HAYIR → create_chat_session(title=soru[:80]) → flush
        ↓
create_chat_message(role=USER, content=soru) → flush
        ↓
answer_question_with_rag(db, org_id, soru, top_k)
  ├── create_embedding(soru) → query vektörü
  ├── cosine_distance sorgusu → top_k chunk
  ├── build_context_from_search_results() → context metni
  └── generate_answer_with_context() → Gemini → cevap
        ↓
create_chat_message(role=ASSISTANT, content=cevap) → flush
        ↓
create_chat_sources(message_id, sources=[...]) → flush
        ↓
db.commit() — tek transaction (session + 2 mesaj + kaynaklar)
        ↓
{
  "session_id": 1,
  "answer": "...",
  "sources": [{"original_file_name": "...", "page_number": ..., ...}]
}

========================================================


========================================================
CELERY ENTEGRASYONU — NEDEN VE NE DEĞİŞTİ?
========================================================

## NEDEN CELERY EKLENDİ?

Bir kullanıcı PDF/DOCX/XLSX yüklediğinde arka planda 3 ağır
iş gerçekleşir:
  1. Dosya parse edilir (pypdf, python-docx, openpyxl)
  2. İçerik chunk'lara bölünür (1200 karakter, 200 overlap)
  3. Her chunk için Gemini API'ye embedding isteği atılır
     (network çağrısı, en yavaş adım)

Bu 3 adım senkron (yani HTTP isteği beklerken) yapılsaydı:
  - API yanıtı 30–120 saniye sürebilir
  - Kullanıcı tarayıcıda donmuş ekran görür
  - Büyük dosyada HTTP timeout hatası alınır
  - Birden fazla kullanıcı aynı anda yüklerse sunucu çöker

Celery ile bu iş yükü arka plan kuyruğuna atılır.
Kullanıcı anında yanıt alır, iş arka planda tamamlanır.

========================================================
EKLENİP/DEĞİŞTİRİLEN DOSYALAR
========================================================

## YENİ DOSYALAR

### backend/app/workers/celery_app.py
  - Celery uygulamasını oluşturur ve yapılandırır.
  - broker=REDIS_URL  : Redis, görevlerin mesaj kuyruğu
  - backend=REDIS_URL : Görev sonuçları da Redis'te saklanır
  - task_serializer="json": Görevler JSON formatında iletilir
  - timezone="Europe/Istanbul": Log'larda doğru saat görünür
  - autodiscover_tasks(["app.workers.tasks"]): tasks.py'daki
    tüm görevleri otomatik keşfeder

### backend/app/workers/tasks.py
  - Celery'nin çalıştıracağı görev fonksiyonlarını içerir.
  - process_document_task(document_id: int):
      1. Yeni bir DB session açar (SessionLocal)
      2. document_processing_service.process_document() çağırır
      3. Dönen document'ın id ve status'ünü dict olarak döner
      4. finally bloğunda session her halükarda kapatılır
  - SessionLocal doğrudan kullanılır çünkü Celery worker'ı
    FastAPI'nin Depends() mekanizmasının dışındadır.
    Her worker kendi session'ını kendisi yönetir.

## DEĞİŞTİRİLEN DOSYALAR

### backend/app/api/v1/documents.py — /upload endpoint'i
  ÖNCESİ (senkron):
    document = create_document_record(...)
    process_document(db, document.id)  ← burada beklerdi
    return document

  SONRASI (asenkron):
    document = create_document_record(...)
    process_document_task.delay(document.id)  ← kuyruğa atar
    return document  ← anında döner

  .delay() metodu görevi Redis kuyruğuna ekler ve hemen döner.
  Kullanıcı status="uploaded" olan document'ı saniyeler içinde
  alır, arka planda işlem devam eder.

### backend/app/services/document_processing_service.py
  (bu servis zaten vardı, Celery'nin çağırdığı yer)
  Akış içi sorumluluklar:
    1. Document status → "processing" yap, commit et
    2. parse_document() → dosyayı section listesine çevir
       (TXT, PDF sayfa sayfa, DOCX tüm paragraflar, XLSX sheet'ler)
    3. create_chunks_from_sections() → 1200 char chunk'lara böl
       (200 char overlap: bağlam kopmasın diye)
    4. create_embeddings() → Gemini API batch çağrısı
       (tüm chunk'ları tek seferde gönderir, verimli)
    5. Eski chunk'ları sil (yeniden işleme güvenliği)
    6. Yeni DocumentChunk kayıtlarını DB'ye ekle
       (content + embedding[1536 boyut] + metadata)
    7. Document status → "completed" yap, commit et
    8. Hata olursa rollback + status → "failed"

### docker-compose.yml
  YENİ SERVIS eklendi: celery_worker
    - Backend ile aynı image'dan build edilir
    - Farklı komutla başlar:
      celery -A app.workers.celery_app.celery_app worker --loglevel=info
    - Backend ile aynı .env ve volume'leri paylaşır
      (uploads klasörüne erişmesi gerekir, dosyayı o okur)
    - depends_on: postgres + redis
      (DB'ye yazar, Redis'ten iş alır)

========================================================
ŞU ANKİ SİSTEM DURUMU — TAMAMLANAN MODÜLLER
========================================================

✅ Kullanıcı kayıt / giriş (JWT tabanlı)
✅ Organizasyon yapısı (user ↔ organization ilişkisi)
✅ Dosya yükleme (PDF, DOCX, XLSX, TXT)
✅ Doküman işleme pipeline'ı (arka planda, Celery ile)
   - Parse → Chunk → Embedding → PostgreSQL'e kayıt
✅ Embedding storage (pgvector, Vector(1536))
✅ Doküman listeleme ve chunk görüntüleme endpoint'leri
✅ Docker ortamı (backend + celery_worker + postgres + redis)
✅ Alembic migration sistemi

🔲 Semantic search (benzer chunk bulma — endpoint var mı?)
🔲 RAG pipeline (soru → embedding → chunk → LLM → cevap)
🔲 Chat geçmişi (model var, servis var, endpoint eksik?)
🔲 Frontend

========================================================
KULLANICI AÇISINDAN GÜNCEL AKIŞ
========================================================

1. KAYIT / GİRİŞ
   POST /api/v1/auth/register  → kullanıcı oluşturulur
   POST /api/v1/auth/login     → JWT token döner

2. DOKÜMAN YÜKLEME
   POST /api/v1/documents/upload  (Bearer token gerekli)
   → Dosya /uploads/ klasörüne UUID isimli kaydedilir
   → documents tablosuna kayıt eklenir (status: "uploaded")
   → Celery kuyruğuna process_document_task görevi atılır
   → Kullanıcıya anında document bilgisi döner

3. ARKA PLANDA (kullanıcı beklemez)
   Celery worker görevi alır →
     - status: "processing"
     - Dosya parse edilir (sayfa/sheet/paragraf bazlı)
     - 1200 char chunk'lara bölünür (200 char overlap)
     - Gemini API ile embedding üretilir (1536 boyut)
     - document_chunks tablosuna kaydedilir
     - status: "completed"
   Hata olursa → status: "failed"

4. DOKÜMAN SORGULAMA
   GET /api/v1/documents          → organizasyondaki tüm dokümanlar
   GET /api/v1/documents/{id}     → tek doküman (status kontrolü)
   GET /api/v1/documents/{id}/chunks → chunk listesi (embedding dahil)

5. SONRAKİ ADIM (henüz tamamlanmadı)
   POST /api/v1/search  → kullanıcı sorusu gelir
     → soru embedding'e çevrilir
     → pgvector cosine similarity ile en yakın chunk'lar bulunur
     → LLM'e (Gemini) "context + soru" gönderilir
     → kaynaklı cevap döner

========================================================
TEKNİK NOTLAR
========================================================

- Celery worker neden ayrı container?
  Ağır iş yükü (embedding API çağrıları) backend ile aynı
  process'te çalışsaydı FastAPI'nin event loop'u bloklanırdı.
  Ayrı container = ayrı process = backend her zaman responsive.

- Neden Redis hem broker hem backend?
  Broker : görev mesajını taşır (producer → queue → consumer)
  Backend: görev sonucunu saklar (status takibi için)
  Tek Redis yeterli, production'da ayırılabilir.

- .delay() vs .apply_async()
  .delay(arg) → basit kullanım, sadece argüman geçer
  .apply_async(args, countdown=10, retry=True) → gelişmiş
  Şu an .delay() yeterli, retry/countdown sonra eklenebilir.

- Chunk overlap neden var?
  1200 char'lık kesimde cümle ortasından kesilirse anlam
  kaybolabilir. 200 char overlap ile her chunk bir öncekiyle
  örtüşür, LLM daha tutarlı bağlam alır.

========================================================

========================================================
DOKÜMAN DURUM TAKIBI VE YENİDEN İŞLEME ENDPOINT'LERİ
— STATUS VE RETRY KATMANININ EKLENMESİ
========================================================

Bu eklemeyle kullanıcılar Celery arka planda çalışırken bir
dokümanın işlem durumunu sorgulayabilir ve başarısız dokümanları
yeniden işlemeye gönderebilir.

Etkilenen dosyalar:
  GÜNCELLEME : schemas/document.py  (DocumentStatusRead şeması eklendi)
  GÜNCELLEME : api/v1/documents.py  (iki yeni endpoint eklendi)

========================================================
SCHEMAS/DOCUMENT.PY — GÜNCELLEME: DocumentStatusRead EKLENDİ
========================================================

class DocumentStatusRead(BaseModel):
    id: int
    status: str
    original_file_name: str
    created_at: datetime

    model_config = {"from_attributes": True}

Neden ayrı şema (DocumentRead yerine)?
  → GET /documents/{id}/status endpoint'i sadece durum bilgisi döner.
    file_path, stored_file_name, organization_id gibi teknik
    alanların tamamı gerekmez — gereksiz veri taşınmaz.
  → Minimal response: kullanıcının ihtiyacı "bu dosya hazır mı?"
    sorusunun cevabıdır. id + status + dosya adı + tarih yeterli.
  → Güvenlik: stored_file_name ve file_path disk yolunu açığa çıkarır.
    Polling (sürekli durum sorgusu) yapan bir endpoint'te bu alanlar
    gereksiz yere ifşa edilmemiş olur.

Dahil edilen alanlar ve neden seçildikleri:
  id              → hangi dokümanı sorguladığını client doğrulayabilir
  status          → "uploaded", "processing", "completed", "failed"
  original_file_name → kullanıcıya hangi dosyanın durumu gösteriliyor
  created_at      → dosyanın ne kadar önce yüklendiği (UI için)

========================================================
API/V1/DOCUMENTS.PY — YENİ ENDPOINT 1:
GET /documents/{document_id}/status
========================================================

@router.get(
    "/{document_id}/status",
    response_model=DocumentStatusRead,
)
def get_document_status(
    document_id: int,
    db: Session = Depends(get_db),
    current_user=Depends(get_current_user),
):

AMAÇ:
  Celery ile arka planda işlenen bir dokümanın güncel durumunu
  döndürür. Frontend bu endpoint'i polling (periyodik sorgulama)
  ile çağırarak "Yüklendi", "İşleniyor", "Tamamlandı" gibi
  aşamaları gösterir.

AKIŞ:
  1. get_current_user_primary_organization_id() → org_id
  2. get_document_by_id_and_organization(document_id, org_id)
     → Bulunamazsa 404 döner
  3. DocumentStatusRead şeması ile filtreli yanıt döner

NEDEN AYRI ENDPOINT?
  → GET /documents/{id} de status döndürür ama DocumentRead şeması
    ile döner (tüm alanlar). Polling senaryosunda her saniye
    büyük bir JSON nesnesi almak yerine sadece durum bilgisi almak
    hem daha hızlı hem daha verimli.
  → Mobil istemci veya Frontend için lightweight endpoint.
  → İleride WebSocket veya Server-Sent Events ile replace edilebilir,
    ama polling için bu endpoint yeterli başlangıç noktası.

TENANT ISOLATION:
  → get_document_by_id_and_organization() iki filtre kullanır:
    document.id ve document.organization_id.
  → Başka organizasyonun doküman ID'si bilinse bile
    404 döner, durum bilgisi sızdırılmaz.

ÖRNEK YANIT:
  {
    "id": 42,
    "status": "processing",
    "original_file_name": "yillik_rapor.pdf",
    "created_at": "2026-05-18T14:30:00"
  }

DURUM GEÇİŞLERİ:
  uploaded   → Celery görevi henüz alınmadı veya kuyrukta
  processing → Celery görevi alındı, parse/chunk/embed sürüyor
  completed  → Tüm chunk'lar ve embedding'ler DB'ye yazıldı
  failed     → Pipeline'da hata oluştu, retry yapılabilir

========================================================
API/V1/DOCUMENTS.PY — YENİ ENDPOINT 2:
POST /documents/{document_id}/retry
========================================================

@router.post(
    "/{document_id}/retry",
    response_model=DocumentRead,
)
def retry_document_processing(
    document_id: int,
    db: Session = Depends(get_db),
    current_user=Depends(get_current_user),
):

AMAÇ:
  status="failed" olan dokümanı yeniden işleme kuyruğuna gönderir.
  Geçici Gemini API hatası, ağ kesintisi veya parse hatası gibi
  durumlarda kullanıcı sistemi manuel yeniden başlatabilir.

AKIŞ:
  1. get_current_user_primary_organization_id() → org_id
  2. get_document_by_id_and_organization(document_id, org_id)
     → Bulunamazsa 404 döner
  3. status == "processing" kontrolü
     → Zaten işleniyorsa 400: "Document is already being processed."
     → Çift işleme önlenir. Aynı doküman için iki Celery görevi
       paralel çalışmaz, race condition engellenir.
  4. document.status = "uploaded" → db.commit() → db.refresh()
     → Durumu sıfırlar: Celery worker işlemi tamamlandı mı yoksa
       yenisi mi başlatıldı bilemeyeceği için "uploaded"a döner.
     → db.commit() burada ayrı yapılır: status güncellemesi
       Celery görevi kuyruğa gitmeden önce DB'ye yazılmalı.
       Worker görevi alınca "uploaded" görmeli, race condition olmasın.
  5. process_document_task.delay(document.id) → Celery kuyruğuna
  6. DocumentRead şeması ile güncellenen document döner

NEDEN processing'i ENGELLIYORUZ?
  → Kullanıcı "retry" endpoint'ini iki kez hızlıca tıklarsa
    ya da arka plan görevi hâlâ çalışıyorken retry atılırsa
    iki Celery worker aynı dokümanı eş zamanlı işler.
  → İkisi aynı anda eski chunk'ları silmeye çalışır → DB tutarsızlığı.
  → Bu kontrol basit ama etkili bir korumadır.

NEDEN status SIFIRLANIYOR?
  → process_document_task başladığında ilk iş status="processing"
    yapmaktır. Eğer belge zaten "failed" durumundaysa ve retry
    kuyruğa girdi ise status "failed" kalmaya devam eder
    (Celery görevi almadan önce). Kullanıcı "retry yaptım ama
    hâlâ failed yazıyor" görmesin diye status="uploaded" ile
    sıfırlanır — Celery alınca "processing"e geçer.
  → Kullanıcıya görsel feedback: status değişti, sistem harekete geçti.

NEDEN DocumentRead DÖNÜYOR (DocumentStatusRead değil)?
  → Retry sonrası kullanıcı tam doküman bilgisini görmek ister
    (dosya adı, boyut, yeni durum). Zaten işlem başlatılıyor,
    ekstra alanların maliyeti kabul edilebilir.
  → GET /status küçük tutuldu, POST /retry daha bilgilendirici
    yanıt veriyor. Uygun asimetri.

ÖRNEK YANIT:
  {
    "id": 42,
    "organization_id": 1,
    "uploaded_by_user_id": 7,
    "original_file_name": "yillik_rapor.pdf",
    "stored_file_name": "a3f7b2c1-...-9d4e.pdf",
    "file_path": "uploads/a3f7b2c1-...-9d4e.pdf",
    "content_type": "application/pdf",
    "file_size": 2048576,
    "status": "uploaded",
    "created_at": "2026-05-18T14:30:00"
  }

========================================================
GÜNCELLENEN URL TABLOSU (DOCUMENTS ROUTER)
========================================================

  POST /api/v1/documents/upload           → dosya yükle (201)
  GET  /api/v1/documents                  → tüm dosyaları listele
  GET  /api/v1/documents/{id}             → tek doküman detayı
  GET  /api/v1/documents/{id}/chunks      → chunk listesi
  GET  /api/v1/documents/{id}/status      → sadece durum bilgisi (YENİ)
  POST /api/v1/documents/{id}/retry       → başarısız dosyayı yeniden işle (YENİ)

========================================================
FRONTEND/CLIENT KULLANIM SENARYOSU
========================================================

NORMAL AKIŞ:
  1. POST /documents/upload → document (status: "uploaded") döner
  2. Her 3 saniyede GET /documents/{id}/status → polling
  3. status "completed" olunca polling durdurulur
  4. GET /documents/{id}/chunks ile chunk'lar gösterilir

HATA AKIŞI:
  1. Polling sırasında status "failed" görülür
  2. Kullanıcıya "Yeniden dene" butonu gösterilir
  3. POST /documents/{id}/retry → status "uploaded"a döner
  4. Normal akış yeniden başlar

NEDEN POLLING (WebSocket değil)?
  → Şu an için yeterli: işlem süresi 5–30 saniye.
  → WebSocket daha karmaşık altyapı gerektirir (connection yönetimi).
  → İleride Server-Sent Events (SSE) veya WebSocket ile değiştirilebilir.
  → 3 saniyelik polling: makul gecikme, sunucu üzerinde kabul
    edilebilir yük.

========================================================

========================================================
COMMIT: "Add usage tracking and admin endpoints"
— KULLANIM TAKİBİ VE ADMIN PANEL KATMANININ EKLENMESİ
========================================================

Bu commit ile sisteme gözlemleme (observability) ve kullanım
takibi (usage tracking) altyapısı eklendi.

Önceki durumda sistem çalışıyordu ama şunu bilmiyorduk:
  - Kim kaç doküman yükledi?
  - Kim kaç chat yaptı?
  - Hangi işlemler ne zaman yapıldı?
  - Doküman işleme başarılı mı, başarısız mı?

Artık bu soruların tümünün cevabı usage_logs tablosunda
saklanıyor ve admin endpoint'leri üzerinden sorgulanabiliyor.

Bu altyapı ileride şunların temelini oluşturacak:
  - Admin panel istatistikleri
  - Organizasyon bazlı faturalandırma
  - Kullanım limiti ve kota yönetimi
  - Denetim kaydı (audit trail)
  - Müşteri bazlı kullanım raporlama

Etkilenen dosyalar (migration hariç):
  GÜNCELLEME : models/__init__.py
  GÜNCELLEME : api/v1/router.py
  GÜNCELLEME : api/v1/documents.py   (upload + retry loglanıyor)
  GÜNCELLEME : api/v1/search.py      (arama loglanıyor)
  GÜNCELLEME : api/v1/chat.py        (soru-cevap loglanıyor)
  GÜNCELLEME : workers/tasks.py      (Celery işleme loglanıyor)
  YENİ       : models/usage.py
  YENİ       : schemas/usage.py
  YENİ       : services/usage_service.py
  YENİ       : api/v1/admin.py
  YENİ       : alembic/versions/7f28c33f65de_add_usage_logs_table.py

========================================================
MODELS/USAGE.PY — YENİ: KULLANIM LOG MODELİ
========================================================

## UsageAction(str, Enum) — Aksion Sabitleri

class UsageAction(str, Enum):
    DOCUMENT_UPLOAD                = "document_upload"
    DOCUMENT_RETRY                 = "document_retry"
    DOCUMENT_PROCESSING_COMPLETED  = "document_processing_completed"
    DOCUMENT_PROCESSING_FAILED     = "document_processing_failed"
    SEMANTIC_SEARCH                = "semantic_search"
    RAG_CHAT                       = "rag_chat"

  str, Enum kombinasyonu neden?
    → str  : DB'ye "document_upload" gibi okunabilir string yazılır.
             JSON'a serialize edilir. "action == 'rag_chat'" karşılaştırması
             doğal çalışır.
    → Enum : "documnt_upload" gibi typo ile geçersiz aksiyon yazılması
             derleme/import aşamasında önlenir.

  Neden 6 farklı aksiyon?
    → DOCUMENT_UPLOAD              : kullanıcı dosya yüklediğinde
    → DOCUMENT_RETRY               : başarısız işleme yeniden denediğinde
    → DOCUMENT_PROCESSING_COMPLETED: Celery worker başarıyla bitirdiğinde
    → DOCUMENT_PROCESSING_FAILED   : Celery worker hata verdiğinde
    → SEMANTIC_SEARCH              : kullanıcı arama yaptığında
    → RAG_CHAT                     : kullanıcı soru sorduğunda

  Bu 6 tip sayesinde:
    "Bu ay kaç dosya işlendi?" → DOCUMENT_PROCESSING_COMPLETED say
    "Hata oranı nedir?"        → FAILED / COMPLETED oranı
    "Kim kaç arama yaptı?"     → SEMANTIC_SEARCH + user_id filtrele
    "En çok chat hangi org?"   → RAG_CHAT + organization_id grupla

## UsageLog(Base) — Kolon Analizi

__tablename__ = "usage_logs"

  id: Mapped[int] — primary_key=True, index=True
    → Her log satırının benzersiz kimliği.

  organization_id: Mapped[int | None]
    ForeignKey("organizations.id", ondelete="SET NULL"), nullable=True
    → Hangi organizasyona ait işlem olduğu.
    → ondelete="SET NULL" neden CASCADE değil?
      Organizasyon silinse bile geçmiş loglar korunur.
      "Bu organizasyon Ocak ayında kaç işlem yapmıştı?" sorusu
      organizasyon silindikten sonra da yanıtlanabilir olmalı.
      Audit trail bütünlüğü için bilinçli karar.
    → nullable=True: Celery worker hata durumunda organization_id
      bilinmiyor olabilir (doküman bulunamadıysa). NULL bırakılır.
    → index=True: "Bu organizasyonun logları" filtre sorgusu için.

  user_id: Mapped[int | None]
    ForeignKey("users.id", ondelete="SET NULL"), nullable=True
    → İşlemi kimin yaptığı.
    → ondelete="SET NULL": Kullanıcı silinse bile log korunur.
    → nullable=True: Celery worker başarısız durumda user_id
      bilinmeyebilir, Sistem tarafından tetiklenen işlemler de olabilir.
    → index=True: "Bu kullanıcının işlemleri" sorgusu için.

  action: Mapped[str]
    String(100), nullable=False, index=True
    → UsageAction.value ile set edilir ("document_upload" gibi).
    → index=True: "Kaç upload var?" gibi filtreli COUNT sorguları
      için kritik. Bu kolon olmadan full table scan olurdu.

  resource_type: Mapped[str | None]
    String(100), nullable=True, index=True
    → İşlemin üzerinde yapıldığı kaynak tipi.
    → Değerler: "document", "search", "chat_session"
    → Örnek: action=RAG_CHAT + resource_type="chat_session"
      → "hangi session'da bu chat yapıldı" kombinasyonu
    → nullable=True: resource kavramı olmayan işlemler için.

  resource_id: Mapped[int | None]
    Integer, nullable=True, index=True
    → İşlemin üzerinde yapıldığı kaynağın DB ID'si.
    → Örnek: document_id=42 → "42 numaralı doküman yüklendi"
    → nullable=True: arama gibi belirli bir kaynak olmayan işlemler.
    → index=True: "Bu dokümanla ilgili tüm logları getir" sorgusu.

  detail: Mapped[str | None]
    Text, nullable=True
    → Serbest metin alanı. İnsan tarafından okunabilir ek bilgi.
    → Örnekler:
        "Uploaded file: sözleşme.pdf"
        "Query: fesih şartları nelerdir?"
        "Question: Bu doküman ne hakkında?"
        "Document processing completed: rapor.pdf"
        "ValueError: Unsupported file type: .exe"  ← hata detayı
    → Text tipi: String(N) değil, uzun hata mesajları sığsın.
    → nullable=True: her log için detay zorunlu değil.

  created_at: Mapped[datetime]
    DateTime, default=datetime.utcnow, nullable=False, index=True
    → Log satırının oluşturulma zamanı.
    → index=True: "Bu hafta ki loglar", "Son 30 günün logları"
      gibi zaman bazlı sorgular çok sık çalışacak.
    → Tüm kolonlar içinde en kritik index bu.

  TOPLAM INDEX SAYISI: 7
    (id, organization_id, user_id, action,
     resource_type, resource_id, created_at)
  Neden bu kadar çok?
    → Raporlama sorguları birden fazla filtreyi kombine kullanır.
      "Bu organizasyonun bu haftaki upload sayısı" gibi:
        WHERE organization_id = ? AND action = ? AND created_at > ?
    → Aylık büyüyen log tablosunda index olmadan bu sorgular
      yavaşlar. Erken index → sonradan tablo büyüyünce sorun yaşanmaz.

========================================================
SCHEMAS/USAGE.PY — YENİ: KULLANIM ŞEMALARI
========================================================

## UsageLogRead — Tek Log Satırı Şeması

class UsageLogRead(BaseModel):
    id: int
    organization_id: int | None
    user_id: int | None
    action: str
    resource_type: str | None
    resource_id: int | None
    detail: str | None
    created_at: datetime
    model_config = {"from_attributes": True}

  model_config = {"from_attributes": True} neden zorunlu?
    → GET /admin/usage/logs endpoint'i SQLAlchemy ORM nesnesi döndürür.
    → Bu ayar olmadan Pydantic sadece dict'ten okuyabilir.
    → Bu ayarla UsageLog ORM nesnesini doğrudan serialize eder.

  Neden tüm alanlar Optional (int | None)?
    → Model tanımında organization_id ve user_id nullable=True.
    → Pydantic şema DB modeliyle uyumlu olmalı.
    → Şemada None kabul etmezse NULL'lu satır serialize edilirken
      ValidationError fırlatır.

## UsageSummary — Platform Geneli Özet Şeması

class UsageSummary(BaseModel):
    total_documents: int
    total_chunks: int
    total_chat_sessions: int
    total_chat_messages: int
    total_usage_logs: int
    document_upload_count: int
    semantic_search_count: int
    rag_chat_count: int
    document_retry_count: int

  Bu şema iki farklı kaynaktan veri toplar:

  KAYNAK 1 — Gerçek tablo sayımları:
    total_documents    → documents tablosu COUNT(*)
    total_chunks       → document_chunks tablosu COUNT(*)
    total_chat_sessions → chat_sessions tablosu COUNT(*)
    total_chat_messages → chat_messages tablosu COUNT(*)
    total_usage_logs   → usage_logs tablosu COUNT(*)

  KAYNAK 2 — usage_logs filtrelemesi:
    document_upload_count  → action = "document_upload" COUNT(*)
    semantic_search_count  → action = "semantic_search" COUNT(*)
    rag_chat_count         → action = "rag_chat" COUNT(*)
    document_retry_count   → action = "document_retry" COUNT(*)

  Tasarım kararı — neden iki kaynak?
    → total_documents: gerçek doküman sayısı (silinmişler dahil değil)
    → document_upload_count: kaç kez upload işlemi yapıldı
    → Bu ikisi farklı anlam taşır. Doküman silinse bile upload logu
      kalır. total_documents azalır ama document_upload_count azalmaz.

  Neden ayrı bir Create şeması yok?
    → UsageSummary asla kullanıcıdan gelen veriden üretilmez.
    → Tamamen hesaplanmış (computed) bir özet. Sadece response şeması.

========================================================
SERVICES/USAGE_SERVICE.PY — YENİ: KULLANIM SERVİS KATMANI
========================================================

## NEDEN AYRI BİR SERVICE?
Log yazma mantığı birden fazla endpoint'ten çağrılır:
  documents.py, search.py, chat.py, workers/tasks.py
Ortak logic tek serviste → DRY (Don't Repeat Yourself) prensibi.
Test: mock ile usage_service patching kolaylaşır.

## create_usage_log(db, action, organization_id, user_id,
                    resource_type, resource_id, detail) → UsageLog

  usage_log = UsageLog(
      organization_id=organization_id,
      user_id=user_id,
      action=action.value,  ← Enum değil string: "document_upload"
      resource_type=resource_type,
      resource_id=resource_id,
      detail=detail,
  )
  db.add(usage_log)
  db.flush()
  return usage_log

  action.value neden?
    → UsageAction.DOCUMENT_UPLOAD bir Enum nesnesidir.
      DB kolonu String(100) bekler.
      .value ile "document_upload" string'i alınır.
    → .value olmadan SQLAlchemy tip uyumsuzluğu hatası fırlatır.

  db.flush() neden (commit değil)?
    → Bu fonksiyon transaction yönetmez. Çağıran endpoint yönetir.
    → documents.py'de: doküman kaydedilir + log yazılır → tek commit.
      İkisi aynı transaction içinde.
    → Yararı: doküman kayıt başarılı ama commit patlarsa log da
      geri alınır. Tutarsız DB durumu oluşmaz (kayıtsız log olmaz).
    → Celery worker'da ise worker kendi commit'ini yönetir.

  Neden tüm parametreler Optional varsayılan değerli?
    → Celery worker hata durumunda organization_id ve user_id
      bilinmeyebilir (doküman DB'de bulunamadıysa).
    → Arama loğunda resource_id yok (aramada tek bir kaynak yok).
    → Esnek imza: her çağrı sadece bildiği alanları doldurur.

## get_usage_logs(db, limit=100) → list[UsageLog]

  db.query(UsageLog)
    .order_by(UsageLog.created_at.desc())
    .limit(limit)
    .all()

  ORDER BY created_at DESC: En yeni log önce gelir.
    → Admin "son ne olmuş?" diye bakar, tarihe göre sıralı liste.

  .limit(limit): Sayfalama değil üst sınır.
    → limit parametresi admin endpoint'inden Query(ge=1, le=500)
      ile kısıtlanır. 0 veya 10000 istenemez.
    → Büyük tabloda limit olmadan tüm kayıtlar çekilirdi → OOM riski.

  Neden sayfalama yok (offset/page)?
    → Şu an admin sadece "son N kaydı göster" ihtiyacı var.
    → İleride büyüyen log tablosu için cursor-based pagination
      veya offset/limit eklenecek teknik borç olarak notlandırıldı.

## get_usage_summary(db) → dict

  total_documents = db.query(Document).count()
  total_chunks    = db.query(DocumentChunk).count()
  total_chat_sessions  = db.query(ChatSession).count()
  total_chat_messages  = db.query(ChatMessage).count()
  total_usage_logs     = db.query(UsageLog).count()

  document_upload_count = (
      db.query(UsageLog)
        .filter(UsageLog.action == UsageAction.DOCUMENT_UPLOAD.value)
        .count()
  )
  # semantic_search_count, rag_chat_count, document_retry_count
  # aynı pattern ile hesaplanır

  return { ... tüm alanlar ... }

  Neden 9 ayrı sorgu?
    → Her COUNT(*) sadece tek tabloyu tarar, hafif sorgu.
    → Alternatif: tek SQL ile GROUP BY + JOIN yapılabilirdi.
      Ama okunabilirlik ve bakım kolaylığı önceliklendirildi.
    → Platform büyüdükçe bu endpoint caching (Redis) ile
      performanslı hale getirilebilir.

  dict döner, UsageSummary döndürmez:
    → FastAPI endpoint'i response_model=UsageSummary ile
      dict'i otomatik validate edip serialize eder.
    → Service katmanı HTTP/Pydantic bilmez — sadece iş mantığı.

========================================================
API/V1/ADMIN.PY — YENİ: ADMİN ENDPOINT'LERİ
========================================================

router = APIRouter(prefix="/admin", tags=["Admin"])

  prefix="/admin" → tüm endpoint'ler /admin/... ile başlar.
  tags=["Admin"]  → Swagger UI'da "Admin" grubu altında görünür.
  Normal kullanıcı bu prefix'i görse bile endpoint'ler
  get_current_superuser ile korunur.

## GET /admin/usage/summary

  @router.get("/usage/summary", response_model=UsageSummary)
  def read_usage_summary(
      db: Session = Depends(get_db),
      current_user = Depends(get_current_superuser),
  ):
      return get_usage_summary(db=db)

  response_model=UsageSummary:
    → Service'ten gelen dict Pydantic ile validate ve serialize edilir.
    → Eksik alan veya tip hatası varsa 500 yerine anlamlı hata.

  Depends(get_current_superuser):
    → users.py'de tanımlı dependency zinciri:
        get_current_superuser → get_current_user (token doğrula)
                              → is_superuser kontrolü (403 yoksa)
    → Normal kullanıcı token'ı geçerli olsa bile 403 alır.
    → Süper admin endpoint'leri için standart koruma deseni.

## GET /admin/usage/logs

  @router.get("/usage/logs", response_model=list[UsageLogRead])
  def read_usage_logs(
      limit: int = Query(default=100, ge=1, le=500),
      db: Session = Depends(get_db),
      current_user = Depends(get_current_superuser),
  ):
      return get_usage_logs(db=db, limit=limit)

  limit: int = Query(default=100, ge=1, le=500):
    → default=100: istemci limit belirtmezse 100 kayıt gelir.
    → ge=1  : 0 veya negatif istenemez.
    → le=500: 501 istenirse FastAPI otomatik 422 döner.
    → Kısıt olmasa kötü niyetli istemci 100000 istek atabilir.

  Neden iki ayrı endpoint (summary ve logs)?
    → summary: dashboard widget için → tek request, hafif
    → logs: ham audit listesi için → limit'li, filtresiz şimdilik
    → İleride logs'a action, user_id, date_from, date_to
      query param filtreler eklenecek.

========================================================
MEVCUT DOSYALARA EKLEME — AKSİYON NOKTALARI
========================================================

Bu milestone'un en önemli tasarım kararı:
Log yazma işlemi merkezi bir middleware'de değil,
her aksiyon noktasında ayrı ayrı çağrılıyor.

Neden middleware değil noktasal log?

  Middleware yaklaşımı:
    → Her HTTP isteğini yakalar (healthcheck, swagger, favicon dahil).
    → Anlamsız gürültü: hangi işlemin iş değeri var bilinmez.
    → Detay bilgisi (dosya adı, sorgu metni) HTTP katmanında yok.

  Noktasal yaklaşım (bu projede):
    → Sadece iş değeri olan 6 aksiyon loglanır.
    → Her log o aksiyonun bağlamını taşır (kim, ne, hangi kaynak, detay).
    → Celery worker da loglayabiliyor (middleware Celery'ye ulaşamaz).

========================================================
DOCUMENTS.PY — DEĞİŞİKLİK: UPLOAD VE RETRY LOGLARI
========================================================

## POST /documents/upload — EKLENDİ

  document = create_document_record(...)
  process_document_task.delay(document.id)   ← Celery kuyruğu

  create_usage_log(                          ← YENİ
      db=db,
      action=UsageAction.DOCUMENT_UPLOAD,
      organization_id=organization_id,
      user_id=current_user.id,
      resource_type="document",
      resource_id=document.id,
      detail=f"Uploaded file: {document.original_file_name}",
  )

  db.commit()    ← tek commit: doküman kaydı + log birlikte

  Neden Celery task'tan önce log yazılıyor?
    → Task asenkron, ne zaman çalışacağı belirsiz.
    → Upload aksiyonu "dosya sisteme alındı" anlamı taşır.
      İşleme tamamlanmasını beklemez.
    → İşleme tamamlanma/hata logları ayrıca Celery worker'da yazılır.

  Neden db.commit() sonda, tek seferde?
    → Doküman kaydı başarılı ama commit patlarsa log da geri alınır.
    → Tutarsız durum: "log var ama doküman yok" veya tam tersi olmaz.

## POST /documents/{id}/retry — EKLENDİ

  document.status = "uploaded"
  db.commit()
  db.refresh(document)
  process_document_task.delay(document.id)

  create_usage_log(                          ← YENİ
      db=db,
      action=UsageAction.DOCUMENT_RETRY,
      organization_id=organization_id,
      user_id=current_user.id,
      resource_type="document",
      resource_id=document.id,
      detail=f"Retried processing for file: {document.original_file_name}",
  )
  db.commit()

  Retry logu neden ayrı bir aksiyonda (DOCUMENT_RETRY)?
    → "Kaç dosya ilk denemede başarısız oldu?" sorusunu yanıtlamak için.
    → DOCUMENT_UPLOAD ile karıştırılmasa denetim açısından önemli.
    → DOCUMENT_RETRY sayısı yüksekse parse/embedding servisinde
      sistemik sorun var demektir → erken uyarı.

========================================================
SEARCH.PY — DEĞİŞİKLİK: ARAMA LOGU
========================================================

  results = semantic_search_chunks(db, org_id, query, top_k)

  create_usage_log(                          ← YENİ
      db=db,
      action=UsageAction.SEMANTIC_SEARCH,
      organization_id=organization_id,
      user_id=current_user.id,
      resource_type="search",
      resource_id=None,     ← aramada tek bir kaynak yok
      detail=f"Query: {search_request.query}",
  )

  db.commit()

  resource_id=None neden?
    → Arama tek bir dokümana değil, tüm org'un chunk'larına yapılır.
    → Kaynak tek bir kayıt değil; resource_id anlamsız olurdu.

  detail alanında sorgu metni neden?
    → "Bu ay en çok aranan terimler neler?" sorusu ileride
      detail alanı parse edilerek yanıtlanabilir.
    → Güvenlik notu: kullanıcı girdisi doğrudan yazılıyor.
      SQL injection riski yok (parameterized query).
      XSS riski yok (API response, HTML değil).

  Neden arama sonuçlarından önce değil sonra loglanıyor?
    → Arama başarısız olursa exception fırlatılır, log yazılmaz.
    → Sadece başarılı aramalar loglanır. Hata loglama için
      ileride except bloğu eklenebilir.

========================================================
CHAT.PY — DEĞİŞİKLİK: RAG CHAT LOGU
========================================================

  rag_result = answer_question_with_rag(...)
  assistant_message = create_chat_message(role=ASSISTANT, ...)
  create_chat_sources(message_id, sources)

  create_usage_log(                          ← YENİ
      db=db,
      action=UsageAction.RAG_CHAT,
      organization_id=organization_id,
      user_id=current_user.id,
      resource_type="chat_session",
      resource_id=chat_session.id,   ← hangi session'da soru soruldu
      detail=f"Question: {chat_request.question}",
  )

  db.commit()   ← session + mesajlar + kaynaklar + log tek commit

  resource_type="chat_session" + resource_id=session.id neden?
    → "Bu session'da kaç soru soruldu?" sorusu log tablosundan
      cevaplanabilir (resource_id filtresi ile).
    → Alternatif olarak chat_messages tablosu sayılabilirdi ama
      log tablosu zaman bilgisi de taşıdığı için daha esnek.

  Bu endpoint'te db.commit() tüm işlemleri kapsar:
    → chat_session (yeni ise)
    → chat_message (kullanıcı)
    → rag_result (LLM çağrısı — DB yazımı yok)
    → chat_message (asistan)
    → chat_sources
    → usage_log
  Hepsi tek transaction. Herhangi biri başarısız olursa
  hiçbiri DB'ye yazılmaz.

========================================================
WORKERS/TASKS.PY — DEĞİŞİKLİK: CELERY WORKER LOGLARI
========================================================

Bu dosya en kritik değişim noktası. Celery farklı bir
process'te çalışır, API'nin DB session'ını paylaşamaz.

@celery_app.task(name="process_document_task")
def process_document_task(document_id: int) -> dict:
    db = SessionLocal()   ← worker kendi session'ını açar

    try:
        document = process_document(db=db, document_id=document_id)

        create_usage_log(             ← YENİ: BAŞARI LOGU
            db=db,
            action=UsageAction.DOCUMENT_PROCESSING_COMPLETED,
            organization_id=document.organization_id,
            user_id=document.uploaded_by_user_id,
            resource_type="document",
            resource_id=document.id,
            detail=f"Document processing completed: {document.original_file_name}",
        )
        db.commit()

        return {"document_id": document.id, "status": document.status}

    except Exception as exc:
        create_usage_log(             ← YENİ: HATA LOGU
            db=db,
            action=UsageAction.DOCUMENT_PROCESSING_FAILED,
            organization_id=None,   ← bilinmiyor olabilir
            user_id=None,           ← bilinmiyor olabilir
            resource_type="document",
            resource_id=document_id,
            detail=str(exc),        ← hata mesajı detay olarak
        )
        db.commit()
        raise

    finally:
        db.close()       ← her durumda bağlantı kapatılır

  Neden except bloğunda organization_id=None, user_id=None?
    → process_document() exception fırlatırsa "document" nesnesi
      exception öncesinde erişilebilir olmayabilir.
    → Özellikle doküman DB'de hiç bulunamazsa document değişkeni
      tanımsız kalır. None kullanmak güvenli.
    → resource_id=document_id (int): bu her zaman bilinir,
      çünkü task parametresi olarak geliyor.

  Neden hata logundan sonra raise?
    → Hata loglandı ama exception yutulmuyor.
    → raise → Celery bu task'ı başarısız olarak işaretler.
    → Celery retry mekanizması devreye girebilir (şu an yapılandırılmadı).
    → Celery flower veya monitoring araçları başarısız task'ları görebilir.

  finally: db.close() neden zorunlu?
    → API endpoint'lerinde get_db() generator'ı finally ile kapatır.
    → Celery worker'da bu otomatizasyon yok, elle kapatılmalı.
    → finally: exception olsa bile, olmasa da çalışır.
    → Kapatılmazsa DB connection pool tükenir, yeni worker'lar
      bağlanamaz hale gelir.

  try → başarı vs try → hata: iki farklı commit neden?
    → Başarı yolunda: process_document commit'i yapar (içeride),
      sonra usage_log flush edilir, dışarıda commit.
      (process_document kendi commit'ini yapıyor olabilir,
       usage_log için ikinci commit gerekiyor.)
    → Hata yolunda: işleme başarısız, ama hata logu kaydedilmeli.
      Ayrı commit ile sadece log yazılır, yarım işleme verileri
      zaten rollback edilmiştir (process_document içinde).

========================================================
MİGRASYON: 7f28c33f65de_add_usage_logs_table.py
========================================================

  Revision    : 7f28c33f65de
  Revises     : 2d176fb1b7b2 (chat history migration'ı)
  Create Date : 2026-05-18 21:06:44

  Bu migration usage_logs tablosunu ve tüm index'lerini oluşturur.

  upgrade() içinde:

  op.create_table("usage_logs",
      sa.Column("id",              sa.Integer(),      nullable=False),
      sa.Column("organization_id", sa.Integer(),      nullable=True),
      sa.Column("user_id",         sa.Integer(),      nullable=True),
      sa.Column("action",          sa.String(100),    nullable=False),
      sa.Column("resource_type",   sa.String(100),    nullable=True),
      sa.Column("resource_id",     sa.Integer(),      nullable=True),
      sa.Column("detail",          sa.Text(),         nullable=True),
      sa.Column("created_at",      sa.DateTime(),     nullable=False),
      sa.ForeignKeyConstraint(["organization_id"], ["organizations.id"],
                               ondelete="SET NULL"),
      sa.ForeignKeyConstraint(["user_id"], ["users.id"],
                               ondelete="SET NULL"),
      sa.PrimaryKeyConstraint("id"),
  )

  7 adet index oluşturuldu:
    ix_usage_logs_action          → filtreli COUNT sorguları
    ix_usage_logs_created_at      → zaman bazlı sorgular
    ix_usage_logs_id              → PK sorguları
    ix_usage_logs_organization_id → org bazlı filtreler
    ix_usage_logs_resource_id     → kaynak bazlı sorgular
    ix_usage_logs_resource_type   → tip bazlı filtreler
    ix_usage_logs_user_id         → kullanıcı bazlı filtreler

  downgrade() içinde (ters sıra zorunlu):
    7 index drop → tablo drop
    Index varken tablo droplanamaz, sıra önemli.

  Çalıştırılan komutlar:
    alembic revision --autogenerate -m "add usage logs table"
    alembic upgrade head

========================================================
API/V1/ROUTER.PY — GÜNCELLEME: ADMIN ROUTER EKLENDİ
========================================================

Önceki hal:
  from app.api.v1 import auth, users, organizations, documents, search, chat

Yeni hal:
  from app.api.v1 import auth, users, organizations, documents,
                          search, chat, admin
  api_router.include_router(admin.router)

  Neden sadece iki satır?
    → admin.router kendi prefix'ini (/admin) kendi dosyasında taşır.
      Router burada sadece birleştirilir.

GÜNCEL URL TABLOSU (TAM LİSTE):
  POST /api/v1/auth/register              → kayıt
  POST /api/v1/auth/login                 → giriş
  GET  /api/v1/users/me                   → profil (token gerekli)
  GET  /api/v1/organizations/me           → organizasyonlar (token gerekli)
  POST /api/v1/documents/upload           → dosya yükle (token gerekli)
  GET  /api/v1/documents                  → dosya listesi (token gerekli)
  GET  /api/v1/documents/{id}             → dosya detayı (token gerekli)
  GET  /api/v1/documents/{id}/chunks      → chunk listesi (token gerekli)
  GET  /api/v1/documents/{id}/status      → işleme durumu (token gerekli)
  POST /api/v1/documents/{id}/retry       → yeniden işle (token gerekli)
  POST /api/v1/search                     → semantik arama (token gerekli)
  POST /api/v1/chat                       → RAG soru-cevap (token gerekli)
  GET  /api/v1/chat/sessions              → oturum listesi (token gerekli)
  GET  /api/v1/chat/sessions/{id}         → oturum detayı (token gerekli)
  GET  /api/v1/admin/usage/summary        → platform özeti (superuser)
  GET  /api/v1/admin/usage/logs           → ham log listesi (superuser)

========================================================
YETKİLENDİRME HİYERARŞİSİ — ÖZET
========================================================

  Token yok          → 401 Unauthorized (tüm korumalı endpoint)
  Token geçersiz     → 401 Unauthorized
  Token geçerli      → normal kullanıcı endpoint'lerine erişir
  is_superuser=False → /admin endpoint'lerine erişemez (403)
  is_superuser=True  → tüm endpoint'lere erişir

  get_current_user        → token doğrula, kullanıcıyı döndür
      ↓ (extends)
  get_current_superuser   → + is_superuser kontrolü

========================================================
BÜYÜK RESİM — KULLANIM TAKİBİ AKIŞ DİYAGRAMI
========================================================

KULLANICI dosya yükler:
  POST /documents/upload
      → create_document_record()  → DB
      → process_document_task.delay()  → Celery kuyruğu
      → create_usage_log(DOCUMENT_UPLOAD)  → DB
      → db.commit()

CELERY WORKER dosyayı işler (arka planda):
  process_document_task()
      → process_document()  → parse + chunk + embed + DB
      → Başarı: create_usage_log(DOCUMENT_PROCESSING_COMPLETED)
      → Hata : create_usage_log(DOCUMENT_PROCESSING_FAILED, detail=hata)
      → db.commit() / db.close()

KULLANICI arama yapar:
  POST /search
      → semantic_search_chunks()
      → create_usage_log(SEMANTIC_SEARCH, detail=sorgu)
      → db.commit()

KULLANICI soru sorar:
  POST /chat
      → answer_question_with_rag()
      → create_chat_message(USER) + create_chat_message(ASSISTANT)
      → create_chat_sources()
      → create_usage_log(RAG_CHAT, resource_id=session.id)
      → db.commit()

ADMİN raporları görür:
  GET /admin/usage/summary  (superuser token ile)
      → get_usage_summary() → 9 COUNT sorgusu → UsageSummary

  GET /admin/usage/logs?limit=200  (superuser token ile)
      → get_usage_logs(limit=200) → list[UsageLogRead]

========================================================
TEKNİK BORÇ / SONRAKI ADIMLAR
========================================================

  1. Admin logs filtreleme:
     Şu an /admin/usage/logs tüm logları döner (sadece limit var).
     İleride: ?action=rag_chat&user_id=5&date_from=2026-01-01
     gibi query param filtreler eklenecek.

  2. Organizasyon bazlı log sorgulama:
     Şu an tüm organizasyonların logları birlikte görünür.
     Multi-tenant admin: her organizasyon sadece kendi loglarını
     görsün şeklinde özelleştirilebilir.

  3. Retry except bloğu iyileştirmesi:
     Celery worker'da except içinde "document" nesnesi
     scope dışında kalabilir (try başında assign edilmedi).
     document_id parametresi kullanılıyor, bu doğru.
     Ama organization_id ve user_id için DB'den tekrar
     sorgu yapılabilir (şu an None bırakılıyor).

  4. Log caching:
     get_usage_summary() her çağrıda 9 COUNT sorgusu çalıştırır.
     Dashboard sık yeniliyorsa Redis cache (TTL: 5 dakika) eklenmeli.

  5. Celery retry sayısı loglama:
     Şu an DOCUMENT_RETRY: kullanıcı manuel retry endpoint'ini
     çağırdığında loglanıyor. Celery otomatik retry mekanizması
     (max_retries) ayrıca loglanmıyor — ileride eklenebilir.

========================================================